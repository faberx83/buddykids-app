import PrivacyView from "@/components/PrivacyView";

// Sotto-pagina dedicata "Privacy e account" (dentro Impostazioni).
//
// TRAMA ONE (24/08/2026) — JSX estratto in components/PrivacyView.tsx per
// essere riusato anche dal guscio NEXTGEN-native, stesso pattern di "Le mie
// prenotazioni" (task #524).
export default function ProfilePrivacyPage() {
  return <PrivacyView />;
}
