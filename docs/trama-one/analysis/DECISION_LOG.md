# TRAMA ONE — Decision Log

Registro delle decisioni vincolanti prese durante l'Impact Assessment (Fase A) e i due Addendum, approvate da Fabrizio. Ogni decisione qui registrata è operativa da subito e vincola gli sprint successivi salvo una nuova decisione esplicita che la sostituisca — mai una correzione silenziosa.

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-01 | Convenzione TRAMA ONE Build Sprint 0-6 | Unica numerazione ufficiale per il lavoro TRAMA ONE: 0 Foundation, 1 Partner onboarding/Admin Review/Walkthrough foundation, 2 Catalogo/prezzo/capacità/Giorni spot Partner, 3 Parent discovery/selezione giorni, 4 Partner response/Booking/Planner Sync, 5 CenterLead/referral/incentivi, 6 Beta/analytics/hardening | Addendum 1 |
| DEC-02 | Conservazione Legacy e Next Gen | Nessuna route, tabella o test Legacy/NextGen viene eliminata, disattivata o indebolita durante TRAMA ONE; convivenza tramite pattern Strangler Fig e route isolate `/one` | Master Prompt + Addendum tecnico finale (§9B) |
| DEC-03 | Sede unica nel pilot | Un solo Centro = una sola sede operativa per il pilot settembre 2026; riuso di `centers.address` esistente | Addendum 1 |
| DEC-04 | Multi-sede differita | Nessuna tabella `locations` in Sprint 0-1; nessun vincolo tecnico che impedisca l'introduzione futura come estensione additiva | Addendum 1 |
| DEC-05 | Offering differita a Sprint 2 | Nessuna nuova entità Offering in Sprint 0; modello canonico `activities`+`activity_weeks`+`activity_days` da adattare, non sostituire; decisione strutturale finale (entità separata sì/no) presa in Build Sprint 2 dopo l'analisi Giorni spot | Addendum 1 |
| DEC-06 | PlannerItem differito a Sprint 4 | Planner resta proiezione calcolata dei dati canonici fino a Sprint 4; rivalutazione della persistenza dedicata basata su state machine Request, idempotenza, accettazioni parziali, giorni confermati, cancellazioni | Addendum 1 |
| DEC-07 | Feature flag server-only | Resolver (`lib/feature-flags/resolve.ts`) con `import "server-only"`, invocabile solo da Server Component/Server Action/layout; nessuna lettura client-side degli override; nessuna variabile `NEXT_PUBLIC_*` con override o regole di rollout | Addendum tecnico finale |
| DEC-08 | Nessuna query flag nel proxy | `proxy.ts` resta scope esclusivo tenant/autenticazione/ruolo/rewrite; il gate `TRAMA_ONE_ENABLED` è applicato nei layout server-side dei tre portali `/one`, non nel proxy | Addendum tecnico finale |
| DEC-09 | Feature Flag Engine obbligatorio nello Sprint 0 | Prerequisito hard, non placeholder; RLS `platform_admin`-only su `feature_flag_overrides`; client `createServiceClient()` (service-role, stesso pattern già in uso per `/internal/beta-pipeline`); fallback a `false` su qualunque errore/timeout/flag sconosciuto | Addendum 1 + Addendum tecnico finale |
| DEC-10 | Deploy/test classificati ADAPT | `deploy.sh` e `test-deploy.sh` corretti da REUSE_AS_IS ad ADAPT nella reuse matrix: `test-deploy.sh` usa oggi `\|\| true` incondizionato su ogni test, violando già oggi CLAUDE.md §3, indipendentemente da TRAMA ONE | Addendum 1 |
| DEC-11 | ONLY_SITEMAP senza deploy | `ONLY_SITEMAP=1 bash deploy.sh` deve essere intercettato prima di git push, `vercel --prod`, alias set, cleanup completo e suite Playwright ordinaria; esegue solo validazione variabili, sitemap, output, apertura opzionale, exit code coerente — nessun nuovo deploy prodotto | Addendum tecnico finale |
| DEC-12 | Trust avanzato differito | Ranking avanzato, benchmark, Trust Score visibile, livelli visibili, verifica AI, gamification, promozioni automatiche restano DEFER per l'intero arco Sprint 0-6, feature-flagged/shadow anche oltre il pilot settembre | Addendum 1 |
| DEC-13 | Walkthrough obbligatorio tra Sprint 1 e Sprint 2 | Non è più opzionale né rinviabile a Sprint 6: motore generico, progresso persistito, benvenuto/completamento profilo/interrompi/riprendi/salta/rilancia/Admin visibility minima in Sprint 1; step attività/settimane/prezzi/Giorni spot/pubblica/dashboard in Sprint 2 | Addendum tecnico finale |
| DEC-14 | Giorni spot negli Sprint 2-4 | Sprint 2: modalità settimana/giornaliera/mista, giorni, capacità, prezzo, minimo giorni, servizi, cancellazioni (Partner). Sprint 3: filtro, dettaglio, selezione giorni, costo, servizi, context (Parent). Sprint 4: accettazione completa/parziale, proposta alternativa, capacità per giorno, Planner Sync per giorno, notifiche, cancellazioni/rimborsi (fulfilment) | Addendum 1 |
| DEC-15 | Nessuna dismissione prima di parità e approvazione esplicita | Nessuna classificazione REMOVE ammessa nella Feature Parity Matrix; una dismissione (REPLACE_AFTER_PARITY) richiede tutte e cinque le condizioni: duplicazione dimostrata, parità funzionale raggiunta, test di regressione Legacy/NextGen verdi, rollback verificato, approvazione esplicita di Fabrizio | Addendum tecnico finale (§9B) |

## Decisioni correlate non elencate esplicitamente da Fabrizio ma derivate e vincolanti

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-16 | Alternativa A per il Feature Flag Engine | Registry TypeScript versionato + override persistiti in tabella, preferita all'Alternativa B (tutto persistito) per minore complessità e rollback via `git revert` | Addendum 1 |
| DEC-17 | Tre route fisiche distinte per `/one` | `app/one/` (Parent), `app/center/one/` (Partner), `app/admin/one/` (Admin) — nessuna route fisica unica può servire i tre tenant per via del rewrite di `proxy.ts` | Addendum 1 |
| DEC-18 | `tests/one/` additiva | La nuova cartella di test TRAMA ONE non sostituisce né duplica gli smoke test esistenti; scope `smoke`/`journeys` realizzati con tag `@smoke`/`@journey` su spec esistenti + nuovi file in `tests/one/` | Addendum tecnico finale |
| DEC-19 | Feature Parity Matrix per capability sufficiente solo per Sprint 0 | Prima di Build Sprint 1 va prodotta una matrice pagina-per-pagina/route-per-route dedicata alle capability coinvolte in quello sprint; da estendere prima di ogni sprint successivo | Addendum tecnico finale |

## Decisioni post Build Sprint 0 (passaggio di consegne operativo)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-20 | Passaggio di consegne operativo a Claude | Da questo momento Claude mantiene lo stato del programma, propone la sequenza, implementa autonomamente le attività tecniche reversibili e interne al repository (lettura, analisi, codice nel perimetro dello sprint approvato, test, documentazione, commit granulari, correzioni tecniche chiaramente individuate), e si ferma solo per i 13 gate espliciti elencati dal comando di passaggio (migrazioni/SQL su Supabase, dati reali, merge su main, deploy/rollback, domini/alias/DNS/Vercel, credenziali/secret, acquisti, migrazioni distruttive, dismissione capability, decisioni di prodotto non ricavabili dai documenti, rischio concreto, test browser sul Mac di Fabrizio) | Comando "TRAMA ONE — PASSAGGIO DI CONSEGNE OPERATIVO A CLAUDE" |
| DEC-21 | Correzione TC-N303/TC-N304: navigazione per path fisico, non per host | I due test smoke Partner/Admin navigavano a `/one` assumendo che il ruolo dell'utente loggato determinasse la route servita; in realtà il routing Partner/Admin è basato sull'HOST (rewrite di `proxy.ts` su `buddykids-partner.vercel.app`/`buddykids-admin.vercel.app`), non esercitato da una suite che gira sempre contro un unico `TEST_BASE_URL`. Corretto navigando ai path fisici `/center/one` e `/admin/one` — decisione tecnica presa autonomamente (correzione di errore tecnicamente individuato, non ambiguità di prodotto), nessun impatto sul comportamento dell'applicazione, solo sulla navigazione del test | Prima esecuzione reale `TEST_SCOPE=smoke bash deploy.sh` in produzione, riportata da Fabrizio |

## Decisioni Build Sprint 1

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-22 | Verifica identità Sprint 1: nota testuale + colonna `document_url` predisposta, upload reale differito | La tabella `center_identity_verifications` (migration_09) include una colonna `document_url` ma questo sprint non collega un upload reale (storage bucket) a quel campo — il center_admin invia una nota testuale, l'Admin decide verified/rejected. Motivazione: mantenere Sprint 1 sul perimetro state-machine/checklist/review/walkthrough senza aggiungere una dipendenza storage aggiuntiva; la colonna è già pronta per non richiedere una nuova migrazione quando il wiring verrà aggiunto (probabile riuso del pattern storage già esistente per `activity_certifications`, riga #17 `FEATURE_PARITY_MATRIX.md`). Nessuna capability rimossa: è uno scope tecnico interno a una capability nuova, non ancora presente in nessuna versione precedente | Decisione tecnica autonoma durante l'implementazione di migration_09, coerente con §4 del passaggio di consegne (alternativa tecnica chiaramente preferibile, nessun impatto prodotto/economico) |
| DEC-23 | Fix bug: coda Admin onboarding escludeva i centri APPROVED | `listCentersForAdminReview()` filtrava solo `SUBMITTED/CHANGES_REQUESTED/CLAIMED/SUSPENDED`, escludendo `APPROVED` — un centro appena approvato spariva subito dalla pagina Admin, rendendo impossibile vederlo per un'eventuale sospensione futura (bottone "Sospendi" già previsto in UI ma mai raggiungibile). Trovato dal test reale end-to-end di Fabrizio (screenshot: centro Partner "Approvato", coda Admin vuota 0/0). Corretto includendo tutti gli stati nella query; la separazione "In revisione" (solo SUBMITTED) vs "Altri stati" resta lato client, invariata | Test reale end-to-end riportato da Fabrizio (screenshot Partner/Admin) |
| DEC-24 | Sitemap non più generata di default ad ogni `test-deploy.sh` | Richiesta esplicita di Fabrizio: il crawl completo (3 portali × 2 browser, centinaia di pagine) su ogni singolo deploy non è giustificato quando non serve verificarla. `test-deploy.sh` ora la esegue solo con `RUN_SITEMAP=1` esplicito; resta disponibile standalone con `ONLY_SITEMAP=1` (nessun deploy necessario). Nessun impatto sui gate di qualità: la sitemap non ha mai bloccato l'esito (best-effort) | Richiesta esplicita di Fabrizio |
| DEC-25 | Nessuna riga `LEAD` creata automaticamente per i centri: gap noto, non risolto in Sprint 1 | Durante la ri-verifica del fix DEC-23, scoperto che nessun centro — né quelli pre-esistenti (per convenzione "riga assente = APPROVED", corretta) né quelli genuinamente creati dopo Sprint 1 (nessun trigger/azione crea una riga `LEAD` alla creazione centro) — può mai raggiungere via UI il bottone "Reclama il mio centro": per testare il flusso è stato necessario un `insert` manuale di una riga `status='LEAD'`. Non corretto in questo sprint (nessun impatto sul pilot, dove i centri sono pochi e gestiti manualmente); da valutare in Sprint 2 se serve automatizzare la creazione della riga `LEAD` per i centri nuovi | Ri-verifica manuale del fix DEC-23 richiesta da Fabrizio |

## Decisioni Build Sprint 1 — Audit Remediation

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-26 | Trigger database (non servizio applicativo) per l'auto-inizializzazione LEAD | Analisi statica ha individuato almeno 4 canali di inserimento in `public.centers`: `app/actions/admin.ts::createCenterAndAssignAction()` (unico punto applicativo runtime), `supabase/seed.sql`, `supabase/seed-test-data.sql` (script SQL diretti, bypassano l'applicazione) e inserimenti manuali via SQL Editor. Nessun punto applicativo unico e obbligatorio esiste per costruzione. Scelto un trigger `AFTER INSERT` su `public.centers` (`migration_10_center_onboarding_auto_lead.sql`, funzione `public.init_center_onboarding_lead()`, SECURITY DEFINER con `search_path` esplicito, `ON CONFLICT DO NOTHING` per idempotenza) invece di un servizio applicativo, per garantire la regola indipendentemente dal client presente e futuro. Supera DEC-25 (gap riconosciuto, non ancora risolto) | Audit esterno Sprint 1, condizione 1 |
| DEC-27 | Linguaggio utente della macchina a stati: valori tecnici invariati, etichette italiane centralizzate | I 6 valori tecnici persistiti (LEAD, CLAIMED, SUBMITTED, CHANGES_REQUESTED, APPROVED, SUSPENDED) restano invariati nel database, nelle funzioni SECURITY DEFINER, nei test di dominio e nell'audit tecnico — nessuna migrazione di traduzione. Creato `lib/onboarding/status-copy.ts` come unico registry tipizzato per le etichette Partner/Admin, differenziate per audience, con CTA e descrizioni approvate esplicitamente da Fabrizio. La CTA "Reclama il mio centro" (e varianti "Rivendica"/"Prendi possesso"/"Claim") è stata rimossa da tutta l'interfaccia (Partner, Admin, storico) e sostituita da "Avvia l'attivazione del centro"; il processo viene raccontato come attivazione e verifica, mai come possesso. `OnboardingClient.tsx` e `AdminOnboardingReviewClient.tsx` non definiscono più etichette proprie — importano esclusivamente dal registry, verificato anche da un test statico di source-scan (TC-N410i) | Audit esterno Sprint 1, condizione 2 |
| DEC-28 | Baseline dei 7 fallimenti preesistenti: identificazione parziale, dato raw non disponibile in repository | I 7 fallimenti Legacy/NextGen/branding indipendenti da TRAMA ONE sono noti qualitativamente per area (login header, sfondo login, nav Gestore, badge/logo NextGen, dashboard Admin) fin da `AUDIT_CHECKPOINT_SPRINT_0.md`, ma l'output Playwright raw (nome test esatto, browser, assertion) non è mai stato conservato come artefatto — solo osservato da Fabrizio e riassunto qualitativamente. Documentato onestamente in `PRE_EXISTING_TEST_FAILURE_BASELINE.md` con i campi non verificabili marcati "DA CONFERMARE", da chiudere al prossimo run completo (Gate 2) invece di inventare dettagli non verificati | Audit esterno Sprint 1, condizione 3 |

## Decisioni di governance operativa (integrazione al passaggio di consegne, post-audit Sprint 1)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-29 | Suite browser completa (`TEST_SCOPE=all`) non più obbligatoria alla chiusura di ogni singolo sprint | Decisione esplicita di delivery di Fabrizio, non una dimenticanza: per Sprint 1 (remediation) e per gli Sprint 2-4 la chiusura richiede solo test statici (tsc/lint/build), unit test, test mirati (auto-LEAD, microcopy, registry) e smoke essenziali delle route toccate — non l'intera suite Playwright. Qualunque errore emerso nei test eseguiti resta comunque bloccante e va corretto, non ignorato. La regressione completa (`TEST_SCOPE=all` confrontata con la baseline) è differita all'Integration Gate dopo Build Sprint 4 | Istruzione esplicita di Fabrizio, integrazione governance post-audit Sprint 1 |
| DEC-30 | Cadenza dell'audit esterno: non più ad ogni sprint, ma un unico Integration Gate dopo Build Sprint 4 | Sostituisce la cadenza precedente (checkpoint + audit esterno + stop ad ogni singolo sprint). Build Sprint 1 (remediation) si chiude con conclusione interna `AUDIT STATUS: READY FOR CONTINUATION` (non richiede condivisione con un audit esterno). Sprint 2, 3, 4 procedono in sequenza autonoma, ciascuno con la propria Feature Preservation Matrix, verifiche statiche/mirate, documentazione e commit granulari, senza fermarsi tra uno sprint e il successivo per chiedere conferma su attività tecniche interne, additive e reversibili. Il primo audit esterno obbligatorio successivo a questa decisione è `AUDIT_CHECKPOINT_INTEGRATION_SPRINT_1_4.md`, a chiusura di Sprint 4, con `TEST_SCOPE=all` eseguita/richiesta e confrontata con la baseline dei fallimenti preesistenti | Istruzione esplicita di Fabrizio, integrazione governance post-audit Sprint 1 |
| DEC-31 | Gate manuali ridotti a 7 categorie esplicite | Applicazione di migrazioni SQL su Supabase; esecuzione SQL diretta; test locale indispensabile (tipicamente browser, da eseguire sul Mac di Fabrizio); merge su main; deploy in produzione; credenziali/secret; rischio concreto per dati o sicurezza. Fuori da queste categorie, il lavoro tecnico interno (codice, migrazioni-come-file non applicate, test statici/unitari, documentazione, commit) procede senza fermarsi per conferma | Istruzione esplicita di Fabrizio, integrazione governance post-audit Sprint 1 |

## Decisioni Build Sprint 2

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-32 | Offering: nessuna nuova entità, estensione additiva di `activities`/`activity_weeks`/`activity_days` (chiude D1 di `ASSUMPTION_LOG.md`) | La riconciliazione AS-IS richiesta prima di Sprint 2 ha verificato che il modello esistente copre già quasi per intero la capability "Giorni spot": disponibilità settimanale/giornaliera, capacità, prezzo, sconto, last-minute, prenotabilità singola, servizi (pre/post, pasto, diete, accesso disabili), regole di cancellazione (per centro) sono tutti già funzionanti in Legacy/NextGen (`activity_weeks`, `activity_days`, `activities.price_per_week`, `centers.cancellation_window_days`), con scrittura Partner già cablata (`saveActivityDaysAction`) e lettura già cablata (`getActivityDays`). Le uniche 2 lacune reali sono: modalità di prenotazione (settimana/giorno/mista) e minimo giorni per prenotazione singola — colmate con 2 colonne additive su `activities`, non con una nuova entità. Creare un'entità Offering separata avrebbe duplicato uno schema già scritto/letto da booking Legacy/NextGen, violando DEC-15 senza beneficio | Riconciliazione Sprint 2, `SPRINT_2_FEATURE_PRESERVATION_MATRIX.md` |

## Verifica reale Gate 1 + Gate 2 (Sprint 1 Audit Remediation)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-33 | Gate 1 (applicazione `migration_10`) e Gate 2 (test browser reali) chiusi con evidenza reale; 3 bug trovati durante il run erano nei *test*, non nel prodotto — corretti senza toccare il codice applicativo | Fabrizio ha applicato `migration_10_center_onboarding_auto_lead.sql` in produzione e rilanciato `tests/one/onboarding-remediation.spec.ts` contro `https://buddykids-app.vercel.app` tre volte, in un ciclo di correzione-riverifica. Primo run: TC-N407/N408 in timeout (`getByLabel("Nome del centro")` non trova nulla perché `NewCenterForm.tsx` non associa label/input con `htmlFor`/`id` né nesting — bug del test, corretto con lo stesso pattern `getByText(...).locator("..")` già in uso altrove nella suite, es. TC-198). Secondo run: stesso timeout spostato sull'assert di successo — `new RegExp('...[TEST]...')` interpretava `[TEST]` come classe di caratteri regex, non testo letterale (confermato leggendo `error-context.md` di Playwright: il messaggio di successo era realmente in pagina) — corretto sostituendo la regex con un confronto testo semplice. Terzo run: TC-N408 (idempotenza) verde, a conferma che il trigger crea correttamente la riga LEAD; TC-N407 falliva ancora per un `.locator("..")` di troppo nel locator della riga (risaliva al contenitore di TUTTE le righe della lista invece che alla singola riga), corretto rimuovendo un livello. Run finale (post fix + redeploy): **8/12 test passed** (TC-N407, TC-N408, TC-N412, TC-N413 × 2 browser), 2 failed attesi (TC-N409, precondizione SQL manuale non ancora eseguita, documentata nel test) e 2 skipped attesi (TC-N411, seconda utenza non provisionata). Nessuna modifica al codice applicativo (`NewCenterForm.tsx`, trigger, registry) durante questo ciclo — solo ai 3 locator/assert del file di test, ogni volta verificato con `tsc --noEmit`/`eslint`/`playwright --list` e committato separatamente (`65248e2`, `64924c0`, `b5842ca`) | Run reali di Fabrizio, Gate 1 + Gate 2, `AUDIT_CHECKPOINT_SPRINT_1.md` §9/§11/§18 |
| DEC-34 | Conflitto noto tra `smoke.spec.ts` (assume `TRAMA_ONE_ENABLED` disattivato di default sugli account di test fissi) e `onboarding-remediation.spec.ts` (richiede lo stesso flag attivo per quegli stessi account, per raggiungere `/admin/one/onboarding` e `/center/one/onboarding`) | I 3 account di test fissi (`TEST_PARENT_EMAIL`, `TEST_CENTER_ADMIN_EMAIL`, `TEST_PLATFORM_ADMIN_EMAIL`, unici usati da `loginAs()`) hanno un override `TRAMA_ONE_ENABLED=true` in `feature_flag_overrides` — necessario e intenzionale per testare l'onboarding, non un residuo da rimuovere. Questo rende i test di `smoke.spec.ts` che verificano il fallback "flag disattivato di default" (TC-N302/303/304/401/402) strutturalmente incompatibili con un run congiunto sugli stessi account: falliscono per costruzione, non per una regressione del prodotto. Deciso di lanciare i due file di test separatamente per questa remediation, non di rimuovere l'override (che romperebbe Gate 2). Non ancora risolto architetturalmente: valutare in futuro se serve un quarto account di test dedicato, senza alcun override, per isolare la verifica "flag off di default" senza questo conflitto | Analisi del run reale di Fabrizio, `tests/fixtures/roles.ts`, `docs/trama-one/analysis/SPRINT_0_ACTIVATION_RUNBOOK.md` §7 |

## Decisioni Build Sprint 2 — chiusura

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-35 | Step Walkthrough attività (Partner) chiude l'ultimo requisito di scope Sprint 2 riusando il motore generico Sprint 1, senza alcuna modifica al motore | `SPRINT_GOVERNANCE.md` richiedeva per Sprint 2 uno "step Walkthrough attività (crea attività/configura settimane/prezzi/Giorni spot/pubblica/dashboard)". Il motore Walkthrough (`lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `app/actions/walkthrough.ts`, `app/one/WalkthroughCard.tsx`), costruito in Sprint 1 con `welcome_parent` come unica dimostrazione, è stato riusato senza modifiche: aggiunta una sola nuova voce `activity_creation_partner` al registry (6 step, testo allineato 1:1 alla frase di scope) e una sola riga di wiring in `app/center/one/page.tsx` (identica al pattern già usato in `app/one/page.tsx` per il Parent — risoluzione `userId` via Supabase, `getWalkthroughProgress(userId, "activity_creation_partner")`, render `<WalkthroughCard progress={...} />`, componente riusato via import diretto da `app/one/WalkthroughCard.tsx`, nessuna duplicazione). Nessuna logica di business duplicata: ogni step del percorso rimanda alla UI reale già esistente (wizard attività, calendario Giorni spot), il percorso è solo una checklist guidata. Con questo, tutti i requisiti di scope Sprint 2 sono coperti: decisione Offering (DEC-05/DEC-32), wizard/disponibilità/prezzo (già AS-IS), Giorni spot Partner con modalità/minimo giorni (migration_11 + UI, TC-414), Walkthrough attività (questa voce, TC-N414/TC-N415). La preview Parent resta esplicitamente fuori scope Sprint 2 (differita a Sprint 3, come già documentato in `SPRINT_2_FEATURE_PRESERVATION_MATRIX.md`) | `SPRINT_GOVERNANCE.md` (scope Sprint 2), riuso del motore Sprint 1 (DEC-13) |

## Gate regressione booking Legacy/NextGen (precondizione Build Sprint 3)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-37 | Suite di regressione booking Legacy/NextGen eseguita e verde — precondizione esplicita di Sprint 3 (GO WITH CONDITIONS #3) soddisfatta con evidenza reale | Fabrizio ha eseguito `tests/genitori/prenotazione.spec.ts` contro produzione tre volte in un ciclo di correzione: primo e secondo run falliti (14/54) per un errore di istruzioni di Claude — il comando dato mancava `TEST_BASE_URL`, quindi Playwright faceva partire il dev server locale (senza Supabase configurato in `.env.local`), il login non completava mai (evidenza: `error-context.md` mostrava testualmente "Supabase non è ancora configurato..."), nessun bug del prodotto. Terzo run, con `source .env.test` + `TEST_BASE_URL=https://buddykids-app.vercel.app` espliciti: **9 passed, 0 failed, 45 skipped** (chromium+mobile-chrome). Gli skip riguardano gli stessi test già noti come dipendenti da precondizioni/dati non presenti in produzione (es. account demo-only, stato prenotazione specifico) — nessuno di essi era tra i 14 falliti nei run precedenti, quindi non è una regressione mascherata da skip. Nessuna modifica al codice applicativo richiesta: l'intero ciclo ha riguardato solo l'istruzione del comando di test, mai il prodotto | Run reali di Fabrizio contro `https://buddykids-app.vercel.app`, comando finale: `source .env.test && TEST_BASE_URL=https://buddykids-app.vercel.app npx playwright test tests/genitori/prenotazione.spec.ts --reporter=list` |

## Incidente reale Sprint 2: dashboard Gestore vuota per ordine invertito codice/migrazione

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-36 | Confermato e chiuso un incidente reale in produzione: `SELECT_COLUMNS` (commit `a7e4cf1`) richiedeva `booking_mode`/`min_days_per_booking` mentre `migration_11` non era ancora applicata — la dashboard Gestore mostrava "Nessuna attività trovata" | Analisi statica dell'ordine dei commit (`a7e4cf1` precede il deploy noto `b5842ca`) ha rivelato che `lib/data/activities.ts::SELECT_COLUMNS` chiedeva esplicitamente le due nuove colonne nella query Supabase prima che la migrazione le creasse — a differenza delle migrazioni precedenti (09/10), qui il codice applicativo NON aveva un percorso tollerante all'assenza delle colonne nella query stessa (i fallback `?? "mixed"`/`?? undefined` in `mapRow()` intervengono solo DOPO che la query è già andata a buon fine). Segnalato a Fabrizio con priorità massima prima di qualunque altro lavoro. Fabrizio ha confermato l'incidente con uno screenshot reale (`/center/activities`: "Nessuna attività trovata per il tuo centro" con l'account di test) e lo ha risolto applicando `migration_11_activity_booking_mode.sql` nello SQL Editor di Supabase in produzione. Post-check reale confermato: `booking_mode` presente con `column_default = 'mixed'::text`; `select booking_mode, count(*) from public.activities group by booking_mode` → 1 riga, `mixed`, count = 11 (tutte le attività esistenti invariate, zero eccezioni). Dopo il fix, le attività reali sono tornate visibili in `/center/activities` (confermato da Fabrizio). Lezione per gli sprint successivi: quando una migrazione introduce colonne già lette da una `SELECT` esplicita (non solo scritte da un'azione), l'ordine sicuro è migrazione-poi-deploy, e questo va verificato contro l'ordine reale dei commit Git PRIMA di dichiarare un batch pronto per il deploy, non dopo | Screenshot reale di Fabrizio (`/center/activities`), verifica SQL post-fix riportata da Fabrizio, `AUDIT_CHECKPOINT_SPRINT_2.md` §1/§6/§11 |

## Decisioni Build Sprint 3 — prima implementazione (Giorni spot Parent)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-38 | Nuova tabella additiva `booking_days` (migration_12) invece di riusare `booking_weeks`; prezzo del giorno congelato al momento della prenotazione; nessun costo navetta su Giorni spot (lacuna nota, non regressione) | Prima fetta di codice di Sprint 3 (dopo chiusura precondizione DEC-37 e riconciliazione `SPRINT_3_FEATURE_PRESERVATION_MATRIX.md`): selezione giorni singoli nella scheda attività (`DetailClient.tsx`, sezione "Giorni spot", visibile solo se `bookingMode !== "week_only"` e ci sono `activity_days` aperti), calcolo prezzo dinamico (`lib/day-pricing.ts`, funzioni pure: tariffa base = `pricePerWeek / 5`, sconto del giorno applicato sopra), propagazione a `/booking/[id]` via `?days=<date,date,...>`, step 1 di `BookingClient.tsx` sostituito da un riepilogo "Giorni scelti" quando la prenotazione è a giorni (`dayBookingMode`), persistenza in una nuova tabella `public.booking_days` (id `activity_day_id` + `booking_id`, PK composita, RLS identica a `booking_weeks`) invece di forzare i giorni dentro `booking_weeks` (semanticamente sbagliato: un giorno non è una settimana) — coerente con DEC-15 (reuse-first: stesso *pattern*, non stessa tabella, quando l'entità è diversa). `booking_days.price` congela il prezzo del giorno al momento della prenotazione (necessario perché `activity_days.discount_percent` può cambiare nel tempo, a differenza di `activities.price_per_week` da cui deriva il prezzo settimanale). Decisioni esplicite di scope, documentate invece di nascoste: (1) nessuna decrementazione automatica di `activity_days.spots_left` alla prenotazione — verificato che lo stesso vale oggi per `activity_weeks.spots_left`/`booking_weeks` (nessun trigger, nessuna decrementazione in `createBookingAction` per le settimane), quindi non è una regressione introdotta qui, ma consistenza con lo stato AS-IS; la capacità/fulfilment per giorno resta esplicitamente Sprint 4; (2) nessun costo navetta (`shuttlePrice`) applicato alle prenotazioni a Giorni spot — `shuttlePrice` è definito solo "a settimana" e nessuna regola di proporzionamento a giorno singolo è mai stata richiesta o definita; "selezione servizi per giorno" resta esplicitamente MANCANTE in `SPRINT_3_FEATURE_PRESERVATION_MATRIX.md`, quindi si è scelto di non applicare alcun costo piuttosto che inventare una tariffa non richiesta; (3) `findWeekOverlapConflicts` (avviso "settimana già impegnata") non è stato esteso ai giorni in questa fetta — resta un controllo solo a livello settimana, gap noto da valutare se serve un equivalente per Giorni spot. Verifiche statiche: `tsc --noEmit` e `next build` puliti (intero progetto), `eslint` pulito sui file toccati. Test Playwright (`tests/genitori/giorni-spot.spec.ts`, TC-N500/TC-N501) gated su `isRealDeployment` E sulla presenza reale di dati Giorni spot per l'attività di test (STEP 7, nuovo, additivo, in `supabase/seed-test-data.sql` — richiede `migration_11` e `migration_12` applicate da Fabrizio, non eseguito da Claude) — se lo STEP 7 non è ancora stato eseguito, i test si limitano a uno skip esplicito invece di fallire a vuoto o dare un falso verde | Prima implementazione Sprint 3, `lib/day-pricing.ts`, `supabase/migration_12_booking_days.sql`, `app/activity/[id]/DetailClient.tsx`, `app/booking/[id]/BookingClient.tsx`/`actions.ts`, `supabase/seed-test-data.sql` STEP 7 |

## Bug latente reale trovato durante Sprint 3 (mascherato da cache incrementale)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-39 | Trovato e corretto un commento con un solo `/` (invece di `//`) in `lib/data/activities.ts`, sintatticamente non valido — mascherato per un numero di sprint non determinato da `tsconfig.tsbuildinfo` (cache incrementale di `tsc`) | Aggiungendo `getActivitiesWithOpenDaySpots()` appena sopra la riga `/ Attività di UN centro specifico...` (mancava il secondo `/`), il primo `npx tsc --noEmit` post-modifica di questa fetta ha fallito con `TS1161: Unterminated regular expression literal` — ma il progetto risultava "pulito" in TUTTE le verifiche precedenti di questa sessione, incluse quelle appena prima di questa modifica. Verificato con `npx tsc --noEmit --module esnext --target es2020` isolato sul solo file: l'errore è reale e preesistente alla mia modifica (la riga rotta esisteva già prima, non introdotta da me). La cache incrementale (`tsconfig.tsbuildinfo`, file tracciato e committato) aveva evidentemente marcato quel file come "già verificato" da un run precedente a quando questo refuso è comparso nel file (causa non identificata con certezza — non è stato possibile determinare quale commit storico l'abbia introdotto senza un'indagine più ampia, non giustificata per la sola correzione di un commento), e nessun `tsc`/`next build` successivo lo ha mai ri-controllato finché una modifica in quello stesso file non ha forzato una ri-verifica reale. Corretto il singolo carattere mancante (`/` → `//`), poi rieseguiti `tsc --noEmit` con `tsconfig.tsbuildinfo` rimosso (forzando una ri-verifica completa, non incrementale) e `next build` con `.next` rimosso (stessa cautela): entrambi puliti, 0 errori, nessun'altra riga con lo stesso pattern trovata altrove nel repository (grep mirato). Lezione per la governance: una verifica statica "pulita" con cache incrementale attiva non è una prova assoluta di correttezza sintattica dell'intero progetto — quando il risultato deve essere riportato come evidenza (es. checkpoint/audit), è più prudente forzare almeno occasionalmente una verifica pulita (buildinfo/cache rimossi) invece di fidarsi sempre della cache | Scoperta durante l'implementazione del filtro Giorni spot in Cerca (Build Sprint 3), verifica isolata con flag `tsc` espliciti, `lib/data/activities.ts` |

## Decisioni Build Sprint 3 — seconda fetta (filtro Ricerca + context object leggero)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-40 | Filtro "Giorni spot" in Ricerca (Legacy+NextGen) implementato riusando `getActivityAvailabilityByWeek()` come precedente di pattern, non come codice condiviso | Nuova funzione `lib/data/activities.ts::getActivitiesWithOpenDaySpots(): Promise<Set<string>>` — una singola query (`select activity_id from activity_days where is_open=true and single_day_bookable=true and spots_left>0`), nessun N+1 per attività, stesso principio della funzione settimanale già esistente ma tabella/colonne diverse quindi funzione dedicata, non parametrizzazione della stessa. Wired in `app/(main)/search/page.tsx` e `app/nextgen/search/page.tsx` (`Promise.all`), passata come `activitiesWithDaySpots` a `SearchClient.tsx`/`SearchDiscoveryClient.tsx`. Nuovo filtro "Solo attività con Giorni spot disponibili" (chip "Giorni spot", pannello dedicato) aggiunto in ENTRAMBI i client con lo stesso pattern degli altri filtri client-side esistenti (`onlyDaySpots` + `daySpotsSet` + branch in `filteredList`/`filteredActivities`, incluso in `activeFiltersCount` e `clearAllFilters()`). In ambiente demo (Supabase non configurato) la funzione ritorna un `Set` vuoto — documentato in UI con una nota esplicativa invece di far credere che il filtro sia rotto | `lib/data/activities.ts`, `app/(main)/search/page.tsx`, `app/nextgen/search/page.tsx`, `SearchClient.tsx`, `SearchDiscoveryClient.tsx` |
| DEC-41 | Context object leggero (source/correlationId) implementato in scope minimo: propagazione via query string end-to-end, log server-side non persistito — NON il sistema eventi/analytics completo (resta E11/Sprint 6) | Riusato `lib/telemetry/correlation.ts` (esistente da Sprint 0, mai collegato al journey Parent prima d'ora): `generateCorrelationId()` chiamato una volta per sessione di ricerca in `SearchClient.tsx` (`source="search"`) e `SearchDiscoveryClient.tsx` (`source="nextgen_search"`). Propagazione: card risultato (`ActivityCardHorizontal.tsx`/`ActivityCard.tsx`, props opzionali `source`/`correlationId` aggiunte in coda, nessun impatto sui chiamanti esistenti che non li passano) → `?source=&cid=` nel link al dettaglio → `DetailClient.tsx` li legge da `useSearchParams()` e li ripropaga in `bookingHref` insieme a `week`/`kid`/`days` già esistenti → `BookingClient.tsx` li legge e li passa a `createBookingAction` → `actions.ts` (`CreateBookingInput.source`/`.correlationId`, entrambi opzionali) chiama `logTelemetryEvent({ event: "booking_created", correlationId, tenant: "family", role: "parent", detail: source ? \`source=${source}\` : null })` dopo la creazione della prenotazione. Nessuna persistenza (solo `console.log` strutturato, come da scope originale di Sprint 0), nessun dato personale nel log (rispettato `TELEMETRY_FORBIDDEN_FIELDS`). Decisione esplicita di scope: non è il "context object" completo previsto da `SPRINT_GOVERNANCE.md` (che cita anche week/child/filters in un oggetto unico) — qui week/kid erano già propagati separatamente da Segment C (`?week=`/`?kid=`), e non sono stati unificati in una struttura dati singola; si è scelto di aggiungere solo il pezzo mancante (source/correlationId) senza un refactor più ampio non richiesto esplicitamente, per restare additivo e a basso rischio. Verifiche statiche: `tsc --noEmit` (buildinfo rimosso, verifica non-incrementale per la lezione DEC-39), `eslint .` (0 errori, warning pre-esistenti invariati) e `next build` puliti sull'intero progetto dopo questa fetta. Test Playwright nuovi: TC-N502/TC-N504 (filtro Giorni spot Legacy/NextGen, gated `isRealDeployment`), TC-N503/TC-N505 (propagazione source/cid card→dettaglio→Prenota ora, stesso gating) | `lib/telemetry/correlation.ts`, `components/ActivityCardHorizontal.tsx`, `components/ActivityCard.tsx`, `app/activity/[id]/DetailClient.tsx`, `app/booking/[id]/BookingClient.tsx`/`actions.ts`, `tests/genitori/cerca.spec.ts`, `tests/nextgen/search-filters-5-7.spec.ts` |

## Decisioni Build Sprint 4 — prima fetta (schema additivo risposta Partner + PlannerItem)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-42 | Schema additivo per la risposta Partner: colonne su `bookings`/`booking_days` (migration_13), nessuna nuova tabella "Request"; nessuna tabella dedicata `planner_items` (D2 chiusa: Planner resta proiezione calcolata) | Le due decisioni di design esplicitamente rinviate al codice da `SPRINT_4_FEATURE_PRESERVATION_MATRIX.md` sono state proposte a Fabrizio e confermate ("ok") prima di scrivere qualunque riga: (a) risposta Partner = colonne additive `partner_decision`/`partner_proposal_note`/`partner_proposed_at`/`responded_at`/`cancelled_by`/`read_by_parent`/`read_by_center` su `bookings` e `partner_decision`/`partner_note`/`capacity_decremented` su `booking_days` (migration_13), WRAP non REPLACE (DEC-15) — riuso letterale del pattern read_by_parent/read_by_center già validato in `activity_inquiries`, nessuna tabella "Request" separata che duplicherebbe dati già presenti in bookings/booking_days; (b) D2 (persistenza PlannerItem, aperta da Sprint 0/DEC-06): decisione NO — nessuna tabella dedicata, `lib/nextgen/planner-insights.ts` continua come proiezione calcolata da bookings/booking_weeks/booking_kids/booking_days, ora estesa (Task #345) a leggere anche `partner_decision` per distinguere confermato/in attesa/proposto. Motivazione: introdurre una tabella `planner_items` avrebbe richiesto un meccanismo di sync/invalidazione (o trigger) tra le tabelle di verità e la proiezione — complessità non giustificata finché il Planner resta un derivato in sola lettura senza stato proprio da mutare direttamente; se in futuro il Planner dovesse acquisire mutazioni dirette (es. riordino manuale non derivabile dalle prenotazioni), la decisione andrebbe riaperta esplicitamente, non silenziosamente. Nuove policy RLS di UPDATE per il centro su entrambe le tabelle (oggi il centro aveva solo SELECT), stesso pattern esatto delle policy già esistenti su `activity_inquiries` — nessuna policy esistente modificata | `SPRINT_4_FEATURE_PRESERVATION_MATRIX.md`, conferma di Fabrizio ("ok"), `supabase/migration_13_booking_partner_response.sql`, DEC-06, DEC-15 |

## Decisioni Build Sprint 4 — correttivo post-deploy (segnalato da Fabrizio)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-43 | Backfill di `partner_decision` per le prenotazioni già confermate prima di migration_13 | Fabrizio ha segnalato (screenshot Inbox Partner) una prenotazione reale già "Confermata" ma mostrata come "Da rispondere". Causa: migration_13 imposta `partner_decision = 'pending'` come default per TUTTE le righe esistenti (comportamento verificato nel proprio POST-CHECK, non un side-effect nascosto) — e siccome `createBookingAction` imposta sempre `status: "confirmed"` incondizionatamente (mai esistito un flusso che scriva `status: "pending"`), ogni prenotazione reale mai fatta sulla piattaforma appare ora come "in attesa di risposta", col rischio concreto che un gestore clicchi "Rifiuta" su una prenotazione già onorata. Decisione: nuova migrazione additiva (`migration_14_bookings_backfill_partner_decision.sql`, solo DML — un UPDATE una tantum, non DDL) che marca come `partner_decision = 'accepted'` (con `responded_at = created_at`, `read_by_center = true`) ogni prenotazione con `status = 'confirmed'` ancora a `partner_decision = 'pending'`, sia su `bookings` sia su `booking_days`. Non tocca la capacità (`spots_left`): `createBookingAction` non l'ha mai decrementata al momento della prenotazione (lo fa solo, da Sprint 4, la risposta esplicita del centro), quindi per queste righe storiche resta correttamente non decrementata. Idempotente (filtro su `partner_decision = 'pending'`), non tocca prenotazioni realmente ancora in attesa (`status != 'confirmed'`, es. lo STEP 8 di test) | Screenshot di Fabrizio (Inbox Partner, post-deploy Sprint 4), `supabase/migration_14_bookings_backfill_partner_decision.sql`, DEC-42 |

## Decisioni Integration Gate (chiusura Build Sprint 1-4, DEC-30)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-44 | Integration Gate NON dichiarato "READY" incondizionato: stato **CONDITIONAL**, con piano di chiusura esplicito prima di considerare Sprint 1-4 formalmente chiusi | Fabrizio ha eseguito il primo run reale `TEST_SCOPE=all` post-Sprint 4 (`logs/deploy-20260727-152424.log`, 44.1 min): 137 failed, 301 skipped, 70 did not run, 374 passed. Analisi riga per riga (vedi `AUDIT_CHECKPOINT_INTEGRATION_SPRINT_1_4.md`): 14 fallimenti riconducibili al debito preesistente noto (ora con dati reali, non più "DA CONFERMARE" — `PRE_EXISTING_TEST_FAILURE_BASELINE.md` aggiornato, la stima storica "7" era sottostimata di un fattore ~2 per non aver contato i 2 browser/progetto; l'area "Admin dashboard" ipotizzata nella baseline non si è invece riprodotta); 12 fallimenti riconducibili a gap già documentati e non bloccanti (DEC-33 TC-N409, DEC-34 conflitto account di test su 5 TC); **97 fallimenti restano non spiegati da nessun documento esistente** e vanno trattati, per la regola esplicita della stessa baseline ("qualunque fallimento fuori dalle aree note è BLOCKER fino a prova contraria"), come potenziali blocker da investigare uno per uno, non da assumere innocui solo perché in aree (NextGen) che TRAMA ONE non ha mai toccato; **70 test non hanno mai prodotto un verdetto** ("did not run") e la causa non è determinabile dal solo log testuale — l'artefatto che l'avrebbe potuta chiarire (`playwright-report/results.json`) era già stato sovrascritto da un run successivo più piccolo nel momento in cui è stata fatta questa analisi, quindi non è oggi possibile identificare quali test specifici siano coinvolti. Non dichiaro quindi "AUDIT STATUS: READY" per onestà verso il principio guida di questo stesso progetto ("mai dichiarare una prova che non esiste") — lo stato resta CONDITIONAL finché non viene eseguito un nuovo run completo con l'archiviazione del report ora in atto (vedi sotto) e i 97+70 casi non sono triagiati singolarmente | `logs/deploy-20260727-152424.log`, `AUDIT_CHECKPOINT_INTEGRATION_SPRINT_1_4.md`, `PRE_EXISTING_TEST_FAILURE_BASELINE.md` |
| DEC-45 | `playwright-report/` viene ora archiviato automaticamente con timestamp (`playwright-report-archive/<timestamp>/`) subito dopo ogni run di `test-deploy.sh`, PRIMA che il run successivo lo sovrascriva | Causa diretta di DEC-44: l'evidenza dettagliata (quali test esatti sono nei "70 did not run") è andata persa perché un run più piccolo successivo ha sovrascritto `playwright-report/results.json` prima che questa analisi potesse essere fatta. Stesso principio già in uso per `logs/deploy-<timestamp>.log` (Task #341): mai più affidarsi a un artefatto che un comando successivo può cancellare silenziosamente. Cartella gitignored, stesso trattamento di `logs/`/`playwright-report/` — nessun impatto su deploy/build/RLS/dati | `test-deploy.sh`, `.gitignore` |

## Decisioni Build Sprint 5 — CenterLead, referral e incentivi

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-46 | Sprint 5 implementa il modello di dominio completo di `TRAMA_Product_Architecture_CX_Handbook_Draft_1.2_Referral_Incentives.md` (§9, §B.2, §B.4 — 8 entità: CenterLead, DemandContext, CenterInvitation, CenterClaim, ReferralAttribution, ReferralReward, PartnerIncentive, IncentiveRuleVersion) in forma volutamente ridotta: **una sola tabella additiva `center_leads`** (migration_17), coerente con `SPRINT_GOVERNANCE.md` ("Artefatti obbligatori: nessuno strutturale oltre lo schema center_leads") e con §B.5 della fonte stessa ("CR-049 può essere implementata in forma minima... CR-051/052 restano feature-flagged e preferibilmente in shadow mode") | DemandContext diventa una colonna `jsonb` (non tabella a sé); CenterInvitation/CenterClaim diventano stati (`suggested→qualified→contacted→claimed\|rejected\|expired`) + colonne (`claimed_center_id`, `claimed_at`) sulla stessa riga, non entità separate — il claim collega un lead a un centro che ha comunque attraversato l'onboarding normale esistente (state machine `center_onboarding_state`, Sprint 1, invariata): nessuna seconda via di pubblicazione (DDL-023 della fonte: "il centro non iscritto è un lead di offerta, non un listing pubblico"). ReferralAttribution è implicita nel first-qualified su `dedupe_key` (normalizzato lato applicativo in `lib/data/center-leads.ts::normalizeDedupeKey`, non con l'estensione Postgres `unaccent` — non installata in questo progetto, coerente con la scelta generale di tenere la logica di dominio in TypeScript puro). ReferralReward/PartnerIncentive/IncentiveRuleVersion diventano campi di sola annotazione manuale (`reward_status`/`reward_note`/`reward_marked_by`/`reward_marked_at`): **nessun calcolo né erogazione automatica di alcun importo reale** — coerente con l'assenza totale di infrastruttura di pagamento nel repository (`CORE_DOMAIN_SOURCE_OF_TRUTH.md` §10) e con lo scope esplicito di Sprint 5 ("mai automatico prima del ledger reale"). Punto di ingresso Genitore: card "Suggerisci un centro" nello stato zero-risultati di Scopri NEXTGEN (Opzione A della fonte, §B.2.1, raccomandata come base del pilot) — crea SOLO una riga `center_leads`, mai un'attività pubblica/prenotabile. Se in futuro il programma economico reale (CR-051/052) dovesse essere implementato, questa decisione va riaperta esplicitamente, non silenziosamente | `SPRINT_GOVERNANCE.md`, `TRAMA_Product_Architecture_CX_Handbook_Draft_1.2_Referral_Incentives.md` §9/§B.2/§B.4/§B.5, `SPRINT_5_FEATURE_PRESERVATION_MATRIX.md`, `supabase/migration_17_center_leads.sql` |

## Decisioni Build Sprint 6 — Capacity (backlog vincolante P1)

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-47 | Servizio canonico di capacità centralizzato in `lib/capacity/service.ts` (`reserveWeekCapacity`/`releaseWeekCapacity`/`releaseAllWeekCapacityForBooking`/`reserveDayCapacity`/`releaseDayCapacity`), sostituendo la logica sparsa in `app/actions/booking-response.ts` e `app/actions/bookings.ts`. `activities.spots_left` (campo editoriale) resta esplicitamente FUORI scope: nessuna derivazione automatica da qui, raccomandazione non bloccante rinviata a un futuro sprint | Costruendo il servizio è emersa una seconda idempotenza mancante oltre a quella già nota (`CORE_DOMAIN_SOURCE_OF_TRUTH.md` §5, decremento settimanale senza flag dedicato): **`cancelBookingAction` (app/actions/bookings.ts) non aveva mai rilasciato la capacità settimanale decrementata all'accettazione** — un genitore che annullava una prenotazione settimanale già accettata perdeva quel posto per sempre, bug distinto e più grave della sola mancanza di idempotenza. Chiuso con `migration_18_capacity_service.sql` (colonna additiva `booking_weeks.capacity_decremented`, simmetrica a `booking_days.capacity_decremented` già esistente da Sprint 4) e wiring di `releaseAllWeekCapacityForBooking` in `cancelBookingAction` prima dell'update di stato. Esteso anche a `updateBookingWeeksAction` (modifica settimane di una prenotazione già accettata): le settimane rimosse vengono rilasciate e quelle aggiunte riservate, stesso principio. `tests/cleanup-test-data.mjs` esteso per resettare `activity_weeks`/`activity_days.spots_left = capacity` sull'attività di test ad ogni run (la delete "grezza" di bookings a fine test non passa da `cancelBookingAction`, quindi senza questo reset lo spots_left di test sarebbe scesso permanentemente nel tempo). Test: `tests/one/capacity.spec.ts` (TC-N607/TC-N608), verifica diretta via client Supabase service_role in sola lettura (nessuna UI mostra `activity_weeks.spots_left` per settimana) | `SPRINT_GOVERNANCE.md` (backlog vincolante Sprint 6, P1 Capacity), `AUDIT_CHECKPOINT_INTEGRATION_SPRINT_1_4.md` §16.6, `supabase/migration_18_capacity_service.sql`, `lib/capacity/service.ts` |
| DEC-48 | Admin visibility + telemetria per gli override scaduti di `feature_flag_overrides` (`/admin/feature-flags`, `lib/feature-flags/evaluate.ts::findRecentlyExpiredMatchingOverride`) | Prima di questa pagina l'UNICO modo di ispezionare o gestire un override era una query SQL manuale (`SPRINT_0_ACTIVATION_RUNBOOK.md`) — ed è esattamente così che TC-N409 (Gate C, ottava ondata) è nato: un override `enabled=true` per `TRAMA_ONE_ENABLED` è scaduto senza che nessuno se ne accorgesse, e un'intera suite di test è diventata rossa prima che la causa fosse trovata. Questa decisione introduce due meccanismi indipendenti: (1) una pagina Admin (`/admin/feature-flags`, RLS-bound, nessun client service_role) che elenca ogni override di ogni flag noto al registry con stato calcolato (`active`/`expiring_soon` entro 72h/`expired`/`no_expiry`) e un badge di allarme quando un override `enabled=true` è scaduto o in scadenza, più azioni CRUD (crea/attiva-disattiva/estendi di N ore/rimuovi scadenza/elimina); (2) un evento di telemetria DISTINTO (`feature_flag_silent_fallback_expired_override`, sempre in `lib/telemetry/correlation.ts`) emesso da `resolveFeatureFlag()` quando il risultato è `false` MA esiste un override `enabled=true` per lo stesso contesto scaduto da meno di 72h — senza mai alterare il valore booleano restituito (il fail-safe esistente resta invariato, questo è solo un segnale aggiuntivo). Nessuna automazione: nessun override viene mai riattivato automaticamente, l'Admin resta l'unico a decidere | `SPRINT_GOVERNANCE.md` (backlog vincolante Sprint 6, P1 Feature flag override expiry), `GATE_C_TRIAGE` (causa radice TC-N409), `lib/feature-flags/evaluate.ts`, `lib/feature-flags/resolve.ts`, `lib/data/feature-flag-overrides.ts`, `app/actions/feature-flag-overrides.ts`, `app/admin/feature-flags/` |
| DEC-49 | Chiusura del debito "email fire-and-forget" (Gap P0 #360 / `CORE_DOMAIN_SOURCE_OF_TRUTH.md` §8) con lo scope minimo esplicitamente richiesto dal backlog vincolante, non un sistema di outbox/queue completo | `lib/email.ts::sendEmail` prima tentava l'invio una sola volta e ignorava qualunque errore in silenzio (nessun log, nessuno stato). Aggiunto: (1) **retry minimo** — un secondo tentativo automatico dopo 400ms se il primo fallisce, prima di arrendersi definitivamente (`SendEmailResult.attempts`, campo additivo che non rompe i call site esistenti che leggono solo `.error`); (2) **logging esplicito** — `console.error` sia nel modulo (`lib/email.ts`, per ogni tentativo fallito) sia nel chiamante (`app/actions/booking-response.ts::notifyParentOfBookingResponse`, con contesto bookingId/decision), a differenza del try/catch silenzioso precedente; (3) **stato di consegna minimo persistito** — 3 colonne additive su `bookings` (`email_delivery_status`: `sent`\|`failed`\|`not_configured`\|`no_recipient`, `email_delivery_error`, `email_delivery_attempted_at`, `migration_19_bookings_email_delivery_status.sql`, non ancora applicata), scritte dopo OGNI tentativo di notifica (sia a livello di intera prenotazione sia per singolo giorno). Deliberatamente FUORI scope: nessuna tabella di log/outbox con storico di ogni invio (solo l'ultimo tentativo per booking), nessuna retry queue asincrona/cron, nessun'altra rotta email (`family.ts`/`invites.ts`/`attendance.ts`/`checkin.ts`) toccata — il backlog è scoped esplicitamente alle notifiche Partner accetta/rifiuta. Test: `tests/one/email-delivery.spec.ts` (TC-N610), verifica end-to-end via client Supabase service_role che lo stato non sia mai più `null` dopo una risposta del centro | `SPRINT_GOVERNANCE.md` riga 151 (backlog vincolante Sprint 6, P2 email fire-and-forget), `CORE_DOMAIN_SOURCE_OF_TRUTH.md` §8, `lib/email.ts`, `app/actions/booking-response.ts`, `supabase/migration_19_bookings_email_delivery_status.sql` |
| DEC-50 | Root cause reale di TC-N414/N415 trovata con evidenza diretta (lettura del codice, non ipotesi, come richiesto esplicitamente dal backlog): race condition client-side in `WalkthroughCard.tsx`, non un problema di dati/harness | Gate C aveva già escluso (quinta ondata) e poi riconfermato residuo (nona/decima ondata, "fallito in modo simile per la seconda run consecutiva nonostante la serializzazione già applicata") che TC-N414/N415 fossero un vero bug applicativo, non rumore. Lettura diretta di `app/one/WalkthroughCard.tsx::handleStart/handleComplete/handleSkip`: i tre handler aggiornavano lo stato locale (`setStepStatus`/`nextStepAfter`) **PRIMA** di attendere la Server Action (`startWalkthroughStepAction`/ecc.) — pattern diverso dalla convenzione già stabilita altrove nel repository (patchare lo stato locale SOLO dopo il successo della action, vedi `CenterLeadsAdminClient.tsx`). `.click()` di Playwright si risolve subito dopo il dispatch dell'evento DOM, non attende la Promise dell'handler onClick: il bottone passava a "Continua" localmente ben prima che l'upsert su `tutorial_progress` arrivasse davvero a Supabase. `page.reload()` (fonte di verità = Server Component + DB) eseguito subito dopo poteva quindi leggere lo stato non ancora persistito e mostrare di nuovo "Inizia" — un vero bug di user-experience anche per un utente reale (rete lenta o errore avrebbero mostrato un progresso mai salvato davvero), non solo un artefatto di test. Fix: i tre handler ora attendono la action e aggiornano lo stato locale solo se `!result.error` (altrimenti loggano l'errore, stesso principio di DEC-49). Corretta anche un'inconsistenza minore trovata nello stesso file (`app/actions/walkthrough.ts`): ogni action revalidava solo `/one` (portale Parent), mai `/center/one` (Partner, motore riusato da Sprint 2) né `/admin/one` — non la causa del bug (le route `/one*` usano già `cookies()`, quindi Next.js le rende dinamiche di default, bypassando la Full Route Cache indipendentemente da `revalidatePath`), ma un'inconsistenza reale per un motore pensato per essere generico multi-tenant, chiusa comunque | `SPRINT_GOVERNANCE.md` riga 152 (backlog vincolante Sprint 6, P2 TC-N414/N415), `GATE_C_TRIAGE_20260728.md` (quinta/nona/decima ondata), `app/one/WalkthroughCard.tsx`, `app/actions/walkthrough.ts` |
| DEC-51 | Command Center Admin (E08, ACR-001/008/015) implementato come AGGREGAZIONE additiva di sole letture già esistenti, non come nuova capability di business né come sostituzione delle pagine per dominio | Handbook Admin 1.1 (§1.2, "Queue-first operations": "ogni dashboard deve rendere espliciti owner, SLA, next action e motivo della priorità") richiede che `/admin/one` (finora un placeholder esplicito — "command center completo resta Sprint 6, fuori scope", Sprint 1) diventi il punto di ingresso prioritario per le code operative Admin. Scelta di scope: **zero nuove tabelle o migrazioni** — `lib/data/command-center.ts::getCommandCenterQueues()` aggrega SOLO funzioni di lettura già esistenti dei sette domini già costruiti (onboarding Sprint 1, prenotazioni/richieste Sprint 4, centri-lead Sprint 5, certificazioni e feedback BETA pre-TRAMA ONE, feature flag Sprint 6 P1) — nessuna nuova query SQL. La classificazione di priorità (alta/media/bassa) è isolata in `lib/command-center/priority.ts`, funzione PURA (nessuna I/O) testabile senza browser: "alta" solo se l'elemento più vecchio in coda supera `QUEUE_STALE_THRESHOLD_DAYS` (3 giorni, stesso ordine di grandezza delle soglie di allarme già scelte in DEC-48) o se sono presenti allarmi feature flag `enabled=true` scaduti/in scadenza (sempre "alta", perché già un incidente silenzioso in corso per costruzione, DEC-48). Le pagine Admin per dominio (`/admin/one/onboarding`, `/admin/bookings`, `/admin/richieste`, `/admin/center-leads`, `/admin/certifications`, `/admin/segnalazioni-beta`, `/admin/feature-flags`) restano invariate e sono l'unico posto dove l'Admin agisce davvero (Separation of duties, Handbook Admin 1.1 §1.2) — il Command Center linka, non duplica, le azioni. La sezione Walkthrough preesistente della stessa pagina (Sprint 1) resta visibile sotto le code, nessuna capability rimossa (DEC-15). Deliberatamente FUORI scope (rimandato oltre Sprint 6, coerente con DEC-12 "Trust avanzato differito"): Trust Score/Partnership Level, filtri salvabili, alert deduplicati via email, azioni rapide dentro il Command Center stesso (oggi solo navigazione) | `SPRINT_GOVERNANCE.md` (backlog Sprint 6, scope E08), `TRAMA_Admin_Product_Architecture_CX_Handbook_Draft_1.1_Trust_Control_Room.md` (§1 Executive Summary/Findings AF-01/AF-02, ACR-001/008/015), `FEATURE_PARITY_MATRIX.md` riga 21 (classificazione ADAPT), `lib/command-center/priority.ts`, `lib/data/command-center.ts`, `app/admin/one/page.tsx` |
| DEC-52 | Eventi analytics con correlationId (E11) implementati come secondo passo OPZIONALE e best-effort sopra `lib/telemetry/correlation.ts` (Sprint 0, invariato), non come sostituzione: `logTelemetryEvent()` continua a scrivere SOLO su console esattamente come prima, `persistProductEvent()` (nuovo, `lib/telemetry/events.ts`) la chiama sempre per prima e poi tenta in più di persistere l'evento su `public.product_events` (`migration_20_product_events.sql`, NON applicata) | `lib/telemetry/correlation.ts` (Sprint 0) aveva già `TelemetryLogEntry`/`TELEMETRY_FORBIDDEN_FIELDS`/`generateCorrelationId()` ma dichiarava esplicitamente "il sistema di eventi/analytics completo (E11, tassonomia eventi, persistenza) resta fuori scope fino a Sprint 6" — nessun evento sopravviveva oltre il log del processo Vercel. `product_events` ha ESATTAMENTE lo stesso shape chiuso di `TelemetryLogEntry` (event_name/correlation_id/tenant/role/detail), nessun campo libero, nessuna colonna owner/userId: stessa filosofia no-PII già stabilita, verificata riga per riga contro `TELEMETRY_FORBIDDEN_FIELDS` prima di scrivere la migrazione. Persistenza sempre "best effort" (stessa filosofia non-bloccante di DEC-49/email e capacity Sprint 5): se Supabase non è configurato, se non c'è sessione autenticata (coerente con la RLS insert policy `auth.uid() is not null`), o se l'insert fallisce per qualunque motivo (incluso — atteso oggi — tabella non ancora esistente in produzione), la funzione logga un `console.error` e ritorna senza mai lanciare: l'osservabilità non deve mai rompere il flusso utente. Whitelist deliberata `KNOWN_PRODUCT_EVENTS` (`lib/telemetry/known-events.ts`, file separato SENZA `import "server-only"` per restare importabile da test puri, stesso principio di `lib/command-center/priority.ts` vs `lib/data/command-center.ts`): un evento non elencato continua solo a essere loggato via console, non viene mai scritto su DB finché qualcuno non lo aggiunge consapevolmente al registro — previene la persistenza silenziosa di un evento futuro non ancora verificato per assenza di PII. Wiring: i tre layout `/one` (`one_route_access`/`one_route_fallback`), l'evento critico DEC-48 (`feature_flag_silent_fallback_expired_override`, ora finalmente interrogabile e non solo visibile per un istante nei log), e quattro nuovi eventi Walkthrough (`walkthrough_step_started`/`_completed`/`_skipped`/`_restarted`, ognuno con un correlationId generato ad hoc perché una Server Action non ha un correlationId di richiesta in ingresso come un Server Component) — questi ultimi pensati esplicitamente per alimentare il prossimo task (#418, "hardening walkthrough", funnel/drop-off). `lib/analytics.ts` (mock-data occupancy/breakdown per le dashboard Legacy/NextGen) resta completamente INVARIATO, come richiesto esplicitamente da `SPRINT_GOVERNANCE.md` ("E11 affianca senza sostituire lib/analytics.ts") — nessuna sovrapposizione: sono due capability distinte (eventi di prodotto vs analytics di dominio mock). Deliberatamente FUORI scope: nessuna pagina Admin di visualizzazione dello stream eventi (valutata, non costruita in questo task — se servirà, seguirà lo stesso precedente di visibilità Admin di DEC-48/DEC-51), nessuna propagazione del correlationId cross-richiesta (cookie), nessuna retention/pulizia automatica (log append-only, nessuna policy di update/delete) | `SPRINT_GOVERNANCE.md` (backlog Sprint 6, scope E11), `lib/telemetry/correlation.ts`, `lib/telemetry/events.ts`, `lib/telemetry/known-events.ts`, `supabase/migration_20_product_events.sql`, `app/one/layout.tsx`, `app/center/one/layout.tsx`, `app/admin/one/layout.tsx`, `lib/feature-flags/resolve.ts`, `app/actions/walkthrough.ts` |

| DEC-53 | CR-050 ("feedback industrializzato", Sprint 6 scope, `SPRINT_GOVERNANCE.md` riga 129) chiusa per RIUSO PURO, zero codice nuovo | Lo scope Sprint 6 per CR-050 era esplicitamente "riuso `beta_feedback`/pipeline esistente" — non una nuova capability. `TRAMA_ONE_Impact_Assessment_v1.0.md` (Sprint 0) aveva già classificato `beta_feedback` + `/internal/beta-pipeline` come REUSE_AS_IS, notando che l'implementazione esistente (floating CTA di segnalazione, pipeline automatica ogni 15 minuti, coda Admin `/admin/segnalazioni-beta`) è "già più matura del necessario per CR-050". Verificato che nessun requisito aggiuntivo del CR (Handbook Referral/Incentives §CR-050, riga 2458: "industrializzare la floating CTA beta e il workflow di feedback contestuale") sia rimasto scoperto: CTA contestuale esiste, triage con owner/SLA esiste (`app/admin/segnalazioni-beta`), nessun centro non verificato pubblicabile (invariante preesistente, non toccata). L'unico elemento nuovo di Sprint 6 che tocca questo dominio è indiretto: la coda "Segnalazioni BETA nuove" ora compare anche in `/admin/one` (Command Center Admin, DEC-51, E08) come ulteriore punto di ingresso prioritario — non una modifica alla pipeline stessa. `FEATURE_PARITY_MATRIX.md` riga 23 aggiornata da "Non avviata" (stato pre-Sprint 6) a "Completata" di conseguenza | `SPRINT_GOVERNANCE.md` riga 129 (scope Sprint 6, CR-050), `TRAMA_ONE_Impact_Assessment_v1.0.md`, `TRAMA_Product_Architecture_CX_Handbook_Draft_1.2_Referral_Incentives.md` riga 2458, `FEATURE_PARITY_MATRIX.md` riga 23, DEC-51 |

| DEC-54 | Hardening Walkthrough (Sprint 6, task #418: "analytics funnel, microcopy, drop-off, accessibilità, performance") chiuso con quattro interventi mirati, nessuna riscrittura del motore generico Sprint 1 | (1) **Funnel/drop-off**: `lib/walkthrough/funnel.ts` (logica PURA, nessuna I/O) calcola per ogni step "raggiunti" (completed+inProgress+skipped da `tutorial_progress`, dato già affidabile dal Sprint 1) e "abbandono vs step precedente" — scelta deliberata di NON basare il funnel principale su `product_events` (E11, migration_20 non ancora applicata): `tutorial_progress` dà lo stesso dato SUBITO, senza dipendere da una migrazione non ancora eseguita da Fabrizio. `product_events` resta comunque usato per l'UNICA metrica che `tutorial_progress` non può dare: il conteggio dei riavvii (`getWalkthroughRestartCount`, best-effort, "N/D" se non disponibile) — un riavvio cancella le righe `tutorial_progress` dell'utente (vedi `restartWalkthroughAction`), quindi è invisibile allo snapshot corrente ma resta un evento storico in `product_events`. Esposto in `/admin/one` (stessa tabella Walkthrough già presente, DEC-51, estesa con due colonne). (2) **Microcopy**: "Salta" → "Salta per ora" (chiarisce che non è definitivo), "Rilancia percorso" → "Ricomincia il percorso" (linguaggio meno gergale), aggiunto "Passo X di Y" accanto al titolo dello step. (3) **Accessibilità**: `role="region"` + `aria-label` sul contenitore, `aria-live="polite"` sul blocco che cambia (nessun annuncio esisteva prima per screen reader al cambio step, WCAG 4.1.3), `aria-describedby` (non `aria-label`) sui pulsanti per dare contesto extra senza alterare il nome accessibile — scelta deliberata: un `aria-label` avrebbe rotto `tests/one/walkthrough-partner.spec.ts` (TC-N414/N415), che individua i pulsanti per nome esatto "Inizia"/"Continua" via `getByRole`. (4) **Performance**: `persistProductEvent()` accetta ora un `context` opzionale (`{ supabase, userId }`) per riusare il client/sessione già ottenuti da `requireUser()` in `app/actions/walkthrough.ts`, eliminando una seconda chiamata `createClient()`+`auth.getUser()` ridondante ad ogni click Walkthrough (4 call site) — comportamento invariato quando il contesto è omesso (retro-compatibile con `resolve.ts`/i tre layout `/one`, che non lo passano) | `SPRINT_GOVERNANCE.md` riga 129 (scope Sprint 6, hardening walkthrough), `lib/walkthrough/funnel.ts`, `lib/walkthrough/data.ts::getWalkthroughRestartCount`, `app/admin/one/page.tsx`, `app/one/WalkthroughCard.tsx`, `lib/telemetry/events.ts::PersistProductEventContext`, `app/actions/walkthrough.ts`, DEC-50, DEC-51, DEC-52 |

| DEC-55 | Chiusura TRAMA ONE Build Sprint 6 — `AUDIT STATUS: READY FOR CONTINUATION` (stessa convenzione di chiusura interna di Sprint 1/2/3/4/5, DEC-30: nessun audit esterno richiesto ad ogni singolo sprint, solo all'Integration Gate già superato dopo Sprint 4) | Scope Sprint 6 (`SPRINT_GOVERNANCE.md` riga 129) completato in tutti i suoi punti: Command Center Admin (E08, DEC-51), feedback industrializzato (CR-050, DEC-53, chiuso per riuso puro), sistema eventi analytics con correlationId (E11, DEC-52), hardening walkthrough (DEC-54) — più i quattro item del backlog vincolante inserito in sede di Evidence Patch (P1 Capacity DEC-47, P1 Feature flag override expiry DEC-48, P2 email fire-and-forget DEC-49, P2 TC-N414/N415 DEC-50). Verifica statica (DEC-29, no `TEST_SCOPE=all` obbligatoria ad ogni sprint): `tsc --noEmit` pulito sull'intero progetto; `eslint` pulito su tutti i file toccati in questo sprint (`lib/`, `app/actions/`, `app/one/`, `app/admin/one/`, `app/center/one/`, `supabase/` non lintato per costruzione — SQL); l'INTERA suite di unit test "no browser" del repository (130 test, tutti i file `tests/**/*.spec.ts`, non solo quelli toccati in questo sprint) verde su chromium e mobile-chrome, confermando nessuna regressione introdotta nei moduli puri esistenti; `BuddyKids_Test_Case.xlsx` ricalcolato senza errori (439 formule). Nessuna riga UI-driven (`isRealDeployment`) eseguita da Claude per costruzione di questa governance — restano da eseguire da Fabrizio nel prossimo `TEST_SCOPE=all` post-deploy: TC-N607/608 (capacity), TC-N609 (feature flags), TC-N610 (email delivery), TC-N611 (command center), TC-N612 (product events), TC-N613 (walkthrough funnel/accessibilità), oltre alla regressione `tests/admin/*` esplicitamente richiesta da `SPRINT_GOVERNANCE.md` riga 137 (nessuna di queste ha un equivalente "no browser": richiedono tutte login reale). Nessuna migrazione applicata da Claude (`migration_18`, `migration_19`, `migration_20` restano da eseguire manualmente da Fabrizio, ciascuna con intestazione "QUESTO FILE NON È STATO APPLICATO AL DATABASE" e sezioni PRE-CHECK/POST-CHECK/ROLLBACK separate). `FEATURE_PARITY_MATRIX.md` righe 21-22 aggiornate da "Non avviata" a "Completata". Reward/incentive (Sprint 5, CenterLead) restano in shadow mode/manuale, invariato, coerente con lo scope MVP settembre 2026 (Definition of Done di Sprint 6, `SPRINT_GOVERNANCE.md` riga 143) | `SPRINT_GOVERNANCE.md` (scope e Definition of Done Sprint 6), DEC-29/30 (convenzione di chiusura interna), DEC-47/48/49/50/51/52/53/54, `FEATURE_PARITY_MATRIX.md` righe 21-22 |

| DEC-56 | Remediation post-gate: primo `TEST_SCOPE=all` dal vivo dopo l'applicazione delle migrazioni 17-20 (Fabrizio, post-DEC-55) ha rivelato 1 bug reale + 1 bug del test, entrambi risolti | Fabrizio ha eseguito `TEST_SCOPE=all bash deploy.sh` (484 test, 272 passed/17 failed/1 flaky) e poi un rerun mirato di `TC-N603`/`TC-N609`. Triage riga per riga contro `GATE_C_TRIAGE_20260728.md`: 15/17 fallimenti + il flaky sono debito preesistente già documentato da settimane (latenza Vercel/Supabase sotto carico), invariato da Sprint 5-6. Solo `TC-N603`/`TC-N609` erano nuovi (prima esecuzione live delle funzionalità Sprint 5-6 col database ora completo) e sono stati investigati con evidenza diretta, non ipotesi: **(1) bug reale** — `getAllCenterLeadsForAdmin()` (`lib/data/center-leads.ts`) usava l'embed PostgREST generico `profiles ( full_name )`, ma `center_leads` ha DUE foreign key verso `profiles` (`suggested_by` e `reward_marked_by`, `migration_17`); l'hint ambiguo fa fallire la query lato PostgREST ("more than one relationship was found"), errore intercettato e tradotto in lista vuota dal fail-safe esistente (mai un errore visibile) — la coda Admin CenterLead risultava quindi SEMPRE vuota nonostante segnalazioni reali nel DB (confermato con query di sola lettura: 6 righe `status='suggested'`, mentre la UI mostrava "Attive (0)"). Fix: `profiles!suggested_by ( full_name )`, stesso pattern già in uso in `lib/data/center-bookings.ts`/`lib/data/inquiries.ts`. **(2) bug del test** — `tests/one/feature-flags.spec.ts` (TC-N609) usava `page.locator("div").filter({hasText:"TRAMA_ONE_ENABLED"}).last()`: poiché più div annidati contengono quella sottostringa (dalla card intera fino al div che avvolge solo il nome del flag), e l'ordine dei match segue l'ordine del documento (antenati prima dei discendenti), `.last()` selezionava il div più interno, privo del `<select>` — confermato leggendo lo snapshot di accessibilità Playwright catturato al fallimento (il `<select>` era presente e funzionante nella pagina reale). Fix: `.first()` per la card (nome flag univoco, sicuro); per la riga dell'override appena creato, filtro combinato `hasText` + `has(bottone "Elimina")` poi `.last()` tra i sopravvissuti, per isolare la riga esatta anche in presenza di un altro override preesistente sullo stesso flag (che ha il proprio bottone "Elimina", altrimenti ambiguo). Nessuna migrazione toccata; entrambi i fix sono modifiche di codice/test, verificate con `tsc --noEmit` ed `eslint` puliti sui file toccati. **Riconfermato dal vivo**: Fabrizio ha rideployato (commit `38fc778`) e rilanciato `TEST_SCOPE=all bash deploy.sh` (484 test) — `TC-N603` e `TC-N609` sono ora entrambi PASSED (nessuna occorrenza nei 15 failed/3 flaky del nuovo run). I restanti 15 fallimenti + 3 flaky coincidono in numero e natura col debito preesistente già isolato in questo stesso ciclo (latenza Vercel/Supabase sotto carico su classi/timeout UI), nessun nuovo fallimento introdotto dai due fix | `lib/data/center-leads.ts`, `tests/one/feature-flags.spec.ts`, `supabase/migration_17_center_leads.sql` (FK verso profiles), `GATE_C_TRIAGE_20260728.md`, DEC-55, `AUDIT_CHECKPOINT_BETA_RELEASE.md` |

## Decisioni Controlled Beta Experience Gate

| # | Decisione | Dettaglio | Fonte |
|---|---|---|---|
| DEC-57 | Blocker §1 del "Controlled Beta Experience, Publication and Readiness Gate" risolto: override globale permanente di `TRAMA_ONE_ENABLED` sostituito da uno scope-cohort a scadenza esplicita, prima di qualunque wiring di navigazione o preparazione deploy | Il gate aveva identificato come hard-stop (non aggirabile) il fatto che `TRAMA_ONE_ENABLED` fosse abilitato in produzione tramite un override `scope=global`, `enabled=true`, `expires_at=null` (introdotto in DEC-11/DEC-30 per la generazione sitemap, mai più corretto) — incompatibile per costruzione con una Beta Controllata (che richiede accesso ristretto a una coorte nota, non globale). Risolto con `supabase/script_controlled_beta_flag_cohort.sql` (committato `ade2e07`, applicato da Fabrizio dopo revisione — non da Claude, coerente con la governance permanente su SQL/produzione): (1) l'override globale storico (id `377b5a2a-e9f5-49cd-957a-ccecf187d64f`) è stato DISABILITATO e marcato scaduto (`enabled=false`, `expires_at=` timestamp di applicazione) tramite UPDATE, non DELETE, per preservare la storia; (2) nuovo override `scope=cohort`, `scope_value='trama-one-controlled-beta'`, `enabled=true`, `expires_at` = +60 giorni (2026-10-02); (3) nuovo override `scope=role`, `scope_value='platform_admin'`, `enabled=true`, `expires_at=null` — verificato PRIMA di aggiungerlo, leggendo `app/one/layout.tsx`/`app/admin/one/layout.tsx`, che nessun bypass strutturale per platform_admin esistesse già (nessuna duplicazione), politica di non-scadenza per questo ruolo dichiarata deliberata; (4) i 2 account di test (Parent/Admin) sono stati aggiunti come righe in `beta_cohort_memberships` (non come override `scope=user` con `expires_at=null` "per comodità" — esplicitamente scartato da Fabrizio) con la STESSA scadenza della coorte, 2026-10-02: `TEST_SCOPE=all` tornerà a fallire su quei percorsi dopo tale data finché la coorte non verrà rinnovata, trade-off esplicito e documentato, non un default silenzioso; (5) nessuna famiglia o centro pilota reale aggiunto in questa fase, per esplicita richiesta di Fabrizio ("nessuna aggiunta, per ora, di famiglie o centri pilot reali"). Verifica indipendente post-applicazione (query di sola lettura via Supabase MCP, non solo l'output incollato da Fabrizio): esattamente 3 righe in `feature_flag_overrides` per il flag (cohort/global/role, nessuna riga spuria) e 2 righe in `beta_cohort_memberships` (i soli 2 account di test, `active=true`, scadenza allineata alla coorte) — stato equivalente riga per riga a quanto atteso e a quanto riportato da Fabrizio. Rollback preparato in due percorsi distinti nello stesso file: una "ROLLBACK SICURO" (default, non ripristina mai `scope=global`/`enabled=true`/`expires_at=null`) e un "RIPRISTINO STORICO DI EMERGENZA" interamente commentato con avviso esplicito, mai eseguibile per errore. Sblocca la ripresa autonoma della sequenza approvata: IA → restyle shell → wiring → Spotlight → visual/mobile acceptance → test critici → deploy controllato | `supabase/script_controlled_beta_flag_cohort.sql`, `lib/feature-flags/evaluate.ts`, `lib/beta-cohorts/membership.ts`, `app/one/layout.tsx`, `app/admin/one/layout.tsx`, `TRAMA_ONE_ROUTE_RELEASE_MATRIX.md`, comando "TRAMA ONE — CORREZIONE DI PRIORITÀ DEL CONTROLLED BETA EXPERIENCE GATE" e "TRAMA ONE — REVISIONE FINALE SCRIPT CONTROLLED BETA" |

| DEC-58 | Information Architecture (§3) chiusa: `/one` e `/center/one` restano `INTERNAL_ONLY` (nessuna voce di menu), `/admin/one` diventa `PRIMARY_NAV` condizionato al flag, `/center/one/onboarding` richiede un redirect/banner condizionato (non ancora cablato), il Walkthrough attività Partner va rimontato su `/center/activities` in fase Spotlight | Verificando il codice reale (non assumendo dalla classificazione §4ter) sono emersi due fatti: (1) `/center-leads` ha GIÀ due accessi legittimi (`CONTEXTUAL_CTA` da ricerca senza risultati + `HubCard` in `ProfileNextgenClient.tsx` righe 124-134, sezione "Attività") — il "gap residuo" dichiarato nella prima stesura del Route Release Matrix (§4bis/§5, "manca un secondo accesso per rivedere lo storico") era un'affermazione non verificata e va corretta: nessuna azione richiesta. (2) `app/center/page.tsx` non controlla mai lo stato del centro per indirizzare un `center_admin` non ancora `APPROVED` verso l'onboarding — la classificazione `POST_LOGIN_REDIRECT` di `/center/one/onboarding` descrive il comportamento CORRETTO, non quello attuale: resta un gap noto da chiudere in fase E (wiring), non ora. Decisioni prese: `/one` (Parent) e `/center/one` (Partner) restano `INTERNAL_ONLY`, NON diventano una 6ª voce della bottom nav Parent (vincolo di design già deliberato nel rebrand Sprint 1, 5 voci fisse) né una voce del menu Partner — il loro contenuto reale oggi non giustifica una destinazione permanente, e diventeranno il punto di montaggio del vero Spotlight (§7-14) invece che destinazioni di navigazione. `/admin/one` (Command Center, DEC-51) è l'unica delle 3 shell che diventa `PRIMARY_NAV`: per funzione aggrega le 7 code operative Admin (coerente con l'Handbook Admin 1.1, "queue-first operations"), da cablare in `app/admin/layout.tsx` SOLO dopo il restyle (task #424) e condizionata a `TRAMA_ONE_ENABLED` risolto server-side nello stesso punto dove il layout già risolve `realRole` (nessuna voce di menu Admin è oggi condizionata al flag: `/admin/center-leads`/`/admin/feature-flags` sono sempre-on perché non gated, questo sarà il primo caso). La `WalkthroughCard` "activity_creation_partner" (Sprint 2) vive oggi solo dentro `/center/one` (route orfana): decisione di rimontarla su `/center/one/activities` in fase Spotlight, non qui, perché quella è la pagina dove il Partner crea davvero un'attività — nessun cambiamento al motore Walkthrough generico, solo a dove il componente viene renderizzato | `TRAMA_ONE_ROUTE_RELEASE_MATRIX.md` §6, `app/nextgen/profile/ProfileNextgenClient.tsx`, `app/center/page.tsx`, `app/center/one/page.tsx`, `app/admin/layout.tsx`, `app/admin/one/page.tsx`, `lib/data/command-center.ts`, DEC-51, DEC-57 |

| DEC-59 | Visual Conformance Audit (§4-6) chiuso: restyle delle 3 shell TRAMA ONE + del componente `WalkthroughCard` condiviso, da inline style/colori hardcoded a classi Tailwind/token già stabiliti — zero cambi di logica, dati o testo verificato dai test | Verificato (`TRAMA_ONE_ROUTE_RELEASE_MATRIX.md` §1-3) che tutte e 3 le shell (`app/one/page.tsx`, `app/center/one/page.tsx`, `app/admin/one/page.tsx`) erano interamente in `style={{}}` inline con `<h1>` di sistema; `app/center/one/page.tsx` aveva un colore hardcoded `#2E86DE`; `app/admin/one/page.tsx` (Command Center, DEC-51) aveva 7+ colori hex hardcoded per i badge priorità — il gap visivo più ampio dell'inventario. Trovato durante il restyle un quarto file con lo stesso difetto non elencato nel Route Release Matrix (componente, non route): `app/one/WalkthroughCard.tsx`, condiviso da `/one` e `/center/one`, stesso colore hardcoded `#2E86DE` per i pulsanti primari. Corretto tutti e 4 riusando ESCLUSIVAMENTE token/pattern già stabiliti altrove (mai inventati): palette badge priorità Admin identica a `FeatureFlagsAdminClient.tsx` (bg-[#FBEAEA]/text-[#C0392B] alta, bg-orange-light/text-trama-orange media, bg-green-light/text-[#2d8f52] bassa), h1 Partner identico a `RichiesteClient.tsx` (`text-xl font-bold text-ink`), header Parent con `PageHeader`/icona brand identico a `SegnalazioniClient.tsx`, CTA primaria `bg-trama-violet` (non più il blu non-brand `#2E86DE`). Aggiunti stati vuoti espliciti dove assenti (Parent/Partner: nessun walkthrough disponibile; Admin: nessuna coda/nessun dato funnel) — prima il contenitore restava silenziosamente vuoto. Preservati con verifica riga per riga: `id`/`aria-*`/nomi accessibili esatti dei pulsanti Walkthrough (`tests/one/walkthrough-partner.spec.ts`, TC-N414/N415), il testo "TRAMA ONE — Parent" (`tests/one/smoke.spec.ts`, TC-N306), e il fatto che `tests/one/command-center.spec.ts` legge le label dai dati (`q.label`) non da stringhe hardcoded nel markup — nessuna modifica ai test necessaria. Verificato inoltre che nessuna route del repository usa `loading.tsx` (grep zero risultati): non introdotto qui per non creare un pattern isolato. Verifica statica: `tsc --noEmit` pulito, `eslint` pulito sui 4 file. Verifica visiva a schermo (screenshot/breakpoint) non eseguibile da Claude in questo ambiente (richiede browser reale contro deploy con flag attivo) — rimandata al Visual Acceptance Gate (§15, task #426) | `TRAMA_ONE_VISUAL_CONFORMANCE.md`, `app/one/page.tsx`, `app/one/WalkthroughCard.tsx`, `app/center/one/page.tsx`, `app/admin/one/page.tsx`, `tailwind.config.ts`, `components/PageHeader.tsx`, `components/dashboard/DashboardLayout.tsx`, `app/admin/feature-flags/FeatureFlagsAdminClient.tsx`, `app/center/richieste/RichiesteClient.tsx`, DEC-51, DEC-58 |

| DEC-60 | Product Walkthrough Spotlight reale (§7-14) chiuso: overlay ancorato a elementi DOM reali, non più la `WalkthroughCard` testuale scollegata dall'interfaccia | Sostituisce, per il solo percorso `activity_creation_partner`, il meccanismo di presentazione stabilito in DEC-13/DEC-35: al posto di una card testuale con pulsanti "Inizia"/"Continua" indipendenti dalla UI applicativa, `components/spotlight/PartnerSpotlight.tsx` disegna un overlay scuro con ritaglio (cutout) attorno all'elemento REALE della pagina corrente (`data-spotlight="..."`, nuovo campo opzionale `spotlightTarget`/`spotlightRoute` in `WalkthroughStepDefinition`/`WalkthroughStepProgress`, popolato solo per i 6 step di `activity_creation_partner`) e un popover ancorato (`lib/spotlight/position.ts`, funzioni pure, nessuna libreria esterna: `computePopoverPosition` sceglie sopra/sotto in base allo spazio disponibile, `computeCutoutRect` calcola il rettangolo con padding, `matchesSpotlightRoute` instrada uno step generico su più route reali con una wildcard singola, es. `/center/activities/*`). L'avanzamento allo step successivo avviene su click GENUINO dell'elemento reale (listener in fase di cattura sul target `data-spotlight`), non su un bottone "Continua" del tour — differenza qualificante rispetto al vecchio meccanismo, richiesta esplicita del gate (§7). Montato una sola volta in `app/center/layout.tsx` (persiste su OGNI pagina Partner, non solo sulla route orfana `/center/one`) ma condizionato in modo additivo a `TRAMA_ONE_ENABLED` risolto server-side per l'utente corrente, stesso resolver già usato da `app/center/one/layout.tsx`: se il flag risolve a `false`, `spotlightProgress` resta `null` e l'overlay non renderizza nulla, nessun impatto sul resto del portale Partner (DEC-02 invariato). `app/center/one/page.tsx` non ospita più la `WalkthroughCard` (rimossa, coerente con DEC-58): resta solo l'entry point verso l'onboarding centro. `welcome_parent` (tutorial Parent) NON ha i nuovi campi popolati — resta intenzionalmente sulla vecchia `WalkthroughCard` testuale, nessuna regressione, puro opt-in per Partner. Limite noto e documentato in testa al componente (non un bug): per gli step `configure_weeks`/`configure_pricing`/`configure_spot_days`/`publish` il completamento è legato al click sull'elemento reale (es. il pulsante "Salva"), non alla conferma che il salvataggio sia andato a buon fine lato server — accettato come compromesso v1, il target resta comunque reale (mai un pulsante decorativo dentro il tour) e l'utente può sempre "Ricomincia il percorso" se necessario. 3 nuovi eventi analytics (`spotlight_shown`/`spotlight_target_not_found`/`spotlight_dismissed`, `lib/telemetry/known-events.ts`, 10 eventi totali da 7) con server action dedicata `logSpotlightEventAction` (`app/actions/spotlight.ts`, fail-safe, stesso pattern di `persistProductEvent`). Un errore ESLint reale (`react-hooks/set-state-in-effect`, non un warning) è stato trovato e corretto durante l'implementazione: la prima chiamata a `measure()` dentro il `useEffect` di ricalcolo posizione chiamava `setState` in modo sincrono nel corpo dell'effetto — corretto schedulandola via `requestAnimationFrame` (con `cancelAnimationFrame` nel cleanup), stesso comportamento, nessuna chiamata sincrona diretta. Verifica statica: `tsc --noEmit` ed `eslint` puliti su tutti i file toccati. Test: `tests/one/spotlight-position.spec.ts` (nuovo, 13 assert/26 con mobile-chrome, "no browser", stesso pattern di `tests/one/feature-flags.spec.ts`) — eseguiti nel sandbox Claude, tutti verdi. `tests/one/walkthrough-partner.spec.ts` riscritto (i vecchi TC-N414/TC-N415 asserivano contro la `WalkthroughCard` ora rimossa da `/center/one`): riscritti contro `/center/activities` (il target reale `create_activity`), aggiunto TC-N416 (nuovo, click reale che avanza il percorso) — richiedono tutti un browser reale contro un deploy con Supabase configurato (`isRealDeployment`), non eseguibili da Claude, da lanciare da Fabrizio. `BuddyKids_Test_Case.xlsx` aggiornato (TC-N414/N415 riscritte, TC-N416/TC-N417 nuove) e ricalcolato senza errori. 9 commit granulari, uno per unità logica (motore posizione+test, registry/data, eventi+action, componente Spotlight, DashboardLayout, layout+rimozione WalkthroughCard, tag DOM reali, test E2E riscritto, xlsx) | `lib/spotlight/position.ts`, `components/spotlight/PartnerSpotlight.tsx`, `lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `lib/telemetry/known-events.ts`, `app/actions/spotlight.ts`, `app/center/layout.tsx`, `app/center/one/page.tsx`, `components/dashboard/DashboardLayout.tsx`, `app/center/activities/page.tsx`, `app/center/activities/[id]/ActivityEditForm.tsx`, `app/center/activities/[id]/calendar/page.tsx`, `tests/one/spotlight-position.spec.ts`, `tests/one/walkthrough-partner.spec.ts`, DEC-13, DEC-35, DEC-58 |

| DEC-61 | Visual Acceptance Gate (§15) instrumentato con `TRAMA_ONE_VISUAL_ACCEPTANCE.md` — gate APERTO, esecuzione riservata a Fabrizio | Non un audit già eseguito (a differenza di DEC-59, Visual Conformance statica): §15 richiede screenshot a 3 breakpoint (mobile 390px/tablet 768px/desktop 1440px) contro un browser reale autenticato su un deploy con Supabase configurato — fuori dal perimetro di ciò che Claude può eseguire in questo ambiente, stessa limitazione già documentata per l'intera suite Playwright UI-driven e per DEC-59 stessa. Prodotta una matrice PASS/FIX/N-A di 20 righe che copre tutto ciò che è stato toccato in §4-6 (le 3 shell + `WalkthroughCard`, task #424/DEC-59) e in §7-14 (il vero Spotlight — overlay/cutout/popover ancorato, badge "target non trovato", dismiss via Escape, avanzamento via click reale — task #425/DEC-60), con precondizioni esplicite (account/coorte, scadenza 2026-10-02 della coorte DEC-57, necessità di un'attività esistente per raggiungere gli step `configure_*`) e una procedura di reporting FIX (screenshot a Claude, correzione solo su evidenza reale, mai "alla cieca"). Il gate resta APERTO finché Fabrizio non esegue la matrice e ogni riga non risulta PASS o N/A motivato: nessuna riga può essere chiusa da Claude senza uno screenshot reale osservato | `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, `TRAMA_ONE_VISUAL_CONFORMANCE.md` (DEC-59), DEC-57, DEC-60 |

| DEC-62 | Controlled Publication procedure (§16-17) prodotta; fase E (wiring) chiusa per `/admin/one` — `app/center/page.tsx` resta deliberatamente non toccato | Durante la stesura della procedura di pubblicazione è stato scoperto un gap reale non ancora chiuso: `app/admin/layout.tsx` non aveva MAI una voce di menu verso `/admin/one` — raggiungibile solo per URL diretto (coerente con TC-N304/TC-N611, che infatti navigano sempre per URL, mai per click su un link di menu), esattamente il gap che DEC-58 aveva descritto come "da cablare... SOLO dopo il restyle" (§4-6, ora chiuso da DEC-59). Chiuso in questo giro: `baseNavItems` reso condizionale, con una nuova voce "Command Center" (`href: "/admin/one"`, icona `ti-target-arrow`) aggiunta SOLO quando `resolveFeatureFlag({flagName:"TRAMA_ONE_ENABLED", tenant:"admin", ...})` risolve `true` per l'utente corrente — stesso resolver già usato da `app/admin/one/layout.tsx` e da `app/center/layout.tsx` per il gate additivo dello Spotlight (DEC-60); nessun'altra voce del menu Admin è mai stata gated, questa è la prima, e resta puramente additiva (routing/redirect delle altre voci invariato). `isNavItemActive()` (`components/dashboard/DashboardLayout.tsx`) non richiede modifiche: tratta `/admin` (radice) con match esatto e ogni altra voce con prefisso, quindi `/admin/one` si evidenzia correttamente senza falsi positivi sulla voce "Dashboard". Aggiunto `TC-N418` (`tests/one/command-center.spec.ts`): verifica che il link compaia con l'href corretto e navighi senza redirect quando il flag è attivo — richiede browser reale, non eseguibile da Claude in questo ambiente, stesso principio di tutta la suite UI-driven `tests/one/*`. **Deliberatamente NON toccato**: il secondo gap di DEC-58 (`app/center/page.tsx` non verifica mai lo stato di approvazione del centro per reindirizzare un `center_admin` non-`APPROVED` verso l'onboarding) resta APERTO — chiuderlo significherebbe modificare la state machine di onboarding Centro (dominio funzionale Sprint 1), esplicitamente vietato dal perimetro di questo gate ("explicitly not reopening Sprint 1-6 functional domain"). Prodotti inoltre: `TRAMA_ONE_CONTROLLED_PUBLICATION.md` (procedura di pubblicazione: readiness checklist §16, 6 passi di pubblicazione §17 — chiusura §15, `TEST_SCOPE=critical` + `tests/one/*` mirati, deploy invariato, verifica live, espansione coorte opzionale, monitoraggio nella finestra di 60 giorni DEC-57 — e rollback a due livelli) e `supabase/script_controlled_beta_expand_cohort.sql` (TEMPLATE non applicato, non compilato con centri/famiglie reali per costruzione: la scelta di CHI arruolare nel pilot è una decisione di prodotto di Fabrizio, non di Claude — pre-check/arruolamento transazionale/post-check/rollback mirato, stesso schema `beta_cohort_memberships` già usato da DEC-57, stessa scadenza allineata alla coorte anziché una scadenza indipendente per persona). Verifica statica: `tsc --noEmit` ed `eslint` puliti; l'intera suite "no browser" del repository verde (nessuna regressione). Nessuna riga della matrice §15 (`TRAMA_ONE_VISUAL_ACCEPTANCE.md`) è stata eseguita da Claude: il gate §16-17 resta condizionato alla sua chiusura da parte di Fabrizio, dichiarato esplicitamente in `TRAMA_ONE_CONTROLLED_PUBLICATION.md` §1 | `app/admin/layout.tsx`, `components/dashboard/DashboardLayout.tsx`, `tests/one/command-center.spec.ts`, `TRAMA_ONE_CONTROLLED_PUBLICATION.md`, `supabase/script_controlled_beta_expand_cohort.sql`, DEC-57, DEC-58, DEC-59, DEC-60, DEC-61 |

| DEC-63 | Pilot Operating Model (§18) prodotto — `CONTROLLED_BETA_OPERATING_MODEL.md`, riuso puro di strumenti già costruiti, un gap operativo reale trovato e documentato (non chiuso: richiede una migrazione, azione di Fabrizio) | Nessuna nuova capability: il modello operativo del pilot si appoggia interamente su `/admin/one` (Command Center, DEC-51), `/admin/feature-flags` (DEC-48), `/admin/segnalazioni-beta` (CR-050, DEC-53) e la sezione Walkthrough/funnel (DEC-54) — cadenza di monitoraggio settimanale (giornaliera dopo il giorno 50, avvicinandosi alla scadenza coorte 2026-10-02, DEC-57), trigger di incidente/rollback (rimanda a DEC-62/`TRAMA_ONE_CONTROLLED_PUBLICATION.md` §4), punto di decisione rinnovo/espansione/stop al giorno ~50, criteri di successo PROPOSTI (non validati, nessun dato storico esiste per questa funzionalità). **Verifica in sola lettura via Supabase MCP (2026-08-03) ha confermato uno stato consistente con DEC-57** (cohort `trama-one-controlled-beta` attiva fino al 2026-10-02, override globale storico disattivato/scaduto, override `role=platform_admin` permanente, 2 membership = i soli 2 account di test) **CORREZIONE (stessa giornata, prima della chiusura di questo task)**: una prima verifica aveva concluso erroneamente che `public.product_events` (migration_20, DEC-52) e le colonne `bookings.email_delivery_status`/`email_delivery_error`/`email_delivery_attempted_at` (migration_19, DEC-49) non esistessero — errore di metodo, non del database: una query con più statement inviata in una sola chiamata `execute_sql` (MCP Supabase) restituisce solo il risultato dell'ULTIMO statement, non di tutti quelli inviati insieme; le prime due query di quel gruppo sono state quindi lette come "nessun risultato" anziché eseguite singolarmente. Ripetendo ogni query singolarmente: entrambe le migrazioni risultano APPLICATE — `product_events` esiste con 46 righe reali (`event_name`: `one_route_access` ×44, `walkthrough_step_started` ×2, nessun evento `spotlight_*` ancora presente perché il codice Spotlight di questa sessione non è ancora deployato), e le 3 colonne email_delivery esistono su `bookings` (1 riga con stato `not_configured` già registrato). `CONTROLLED_BETA_OPERATING_MODEL.md` §4 corretto di conseguenza: il quadro di monitoraggio è già utilizzabile oggi, nessuna azione di Fabrizio richiesta per questo. Lezione operativa per il resto del programma: mai inviare più statement SQL in una singola chiamata `execute_sql` se serve leggere il risultato di ciascuno — un caso per query, sempre | `CONTROLLED_BETA_OPERATING_MODEL.md`, DEC-48, DEC-49, DEC-51, DEC-52, DEC-53, DEC-54, DEC-57, DEC-60, DEC-62 |

| DEC-64 | Production Hygiene (§19) chiuso: script SQL sola lettura riutilizzabile + script di pulizia separato (non applicato) per 2 centri e 7 center_leads di test; correzione formale della DEC-63 | Verifica in sola lettura via Supabase MCP (2026-08-03), stavolta con una query per chiamata (mai più statement insieme — lezione diretta dell'errore descritto sotto). Trovati 9 record di debris inequivocabile, tutti creati lo stesso giorno con nomi contenenti "[TEST]"/timestamp epoch: 2 centri orfani (`[TEST] Centro Auto LEAD 1785752525740` id `9e284123-cfac-424d-8fe9-e55d9d7a4d4f`, `[TEST] Centro Idempotenza 1785752529922` id `e8fd53eb-2020-446f-93c7-1f695140149f`, entrambi 0 attività/0 audit) e 7 righe `center_leads` (`[TEST] Centro Segnalato <timestamp>`, 6 `suggested`/1 `claimed` verso `[TEST] Centro BuddyKids`). Distinti esplicitamente dai fixture legittimi che NON vanno toccati: `[TEST] Centro BuddyKids` (id `40a64d60-3d45-4851-bac4-1761915ad92e`, reale/permanente, 2 attività/14 audit, collegato a `TEST_CENTER_ADMIN_EMAIL`) e `Test centro estivo` (id `3a240835-6412-402c-9c63-2c8cf0944fca`, predata TRAMA ONE, 1 attività, ambiguo — lasciato esplicitamente alla decisione di Fabrizio, non incluso nella pulizia). Verificate anche `feature_flag_overrides`/`beta_cohort_memberships` per `TRAMA_ONE_ENABLED`: nessuna riga spuria, stato identico a quanto atteso da DEC-57. Prodotti `supabase/script_production_hygiene_audit.sql` (sola lettura, riutilizzabile ad ogni ciclo di monitoraggio §18) e `supabase/script_production_hygiene_cleanup.sql` (non applicato, perimetro chiuso ai 9 record sopra, PRE-CHECK/transazione/POST-CHECK, nessun rollback SQL possibile dopo il commit — solo Point-in-Time Recovery nativo Supabase come rete di sicurezza residua) e `TRAMA_ONE_PRODUCTION_HYGIENE.md`. **Correzione formale della DEC-63**: quella voce del log conteneva la stessa falsa affermazione già corretta in `CONTROLLED_BETA_OPERATING_MODEL.md` §4 (migrazioni 19/20 erroneamente dichiarate non applicate, per un errore di lettura di una chiamata `execute_sql` multi-statement, che restituisce solo il risultato dell'ultimo statement) — il testo di DEC-63 è stato aggiornato in questo stesso log per riportare i fatti corretti, invece di lasciare una voce storica falsa senza annotazione, seguendo lo stesso principio già applicato da DEC-58 per una correzione precedente. Nessuna azione applicata al database da Claude in nessuna parte di questo task, coerente con la governance permanente | `supabase/script_production_hygiene_audit.sql`, `supabase/script_production_hygiene_cleanup.sql`, `TRAMA_ONE_PRODUCTION_HYGIENE.md`, `CONTROLLED_BETA_OPERATING_MODEL.md`, DEC-49, DEC-51, DEC-52, DEC-57, DEC-58, DEC-63 |

| DEC-65 | Output finale e gate GO/NO-GO (§20) chiuso: verdetto **GO WITH CONDITIONS** — `TRAMA_ONE_CONTROLLED_BETA_GATE_FINAL.md` | Consolida §1-§19 in un unico verdetto, come richiesto dall'ultima sezione del gate "Controlled Beta Experience, Publication and Readiness Gate". Non GO pieno: 3 condizioni restano aperte, tutte eseguibili solo da Fabrizio (mai da Claude in questo ambiente) — (1) eseguire `TRAMA_ONE_VISUAL_ACCEPTANCE.md` (§15, screenshot reali a 3 breakpoint, matrice PASS/FIX/N-A ancora aperta); (2) lanciare `TEST_SCOPE=critical` + `tests/one/*.spec.ts` contro un deploy reale (copre in particolare TC-N414/N415/N416 Spotlight e TC-N418 wiring Command Center, nessuno mai eseguito contro un browser vero); (3) decidere se/quando eseguire `script_production_hygiene_cleanup.sql` (§19, non applicato) e cosa fare di `Test centro estivo` (ambiguo, DEC-64). Non NO-GO: nessun blocco tecnico reale trovato, nessuna regressione, ogni gate statico/di codice chiuso e verificato (§1-§14, §18-§19 chiusi; §16-17 prodotta ma non eseguita, per costruzione — richiede §15 chiuso prima). Riepilogati in un'unica tabella gli stati di ogni sezione, l'elenco completo dei deliverable prodotti in questo gate (documenti, voci DEC-57..DEC-64, codice, test, script SQL), ciò che è stato deliberatamente escluso per rispettare il perimetro (nessuna riga di `app/center/page.tsx` toccata, nessun nuovo business epic, nessuna migrazione applicata da Claude, nessun materiale di comunicazione pilota) e lo stato del repository (29 commit granulari da `bd99f41` a `42c4eea`, working tree pulito). Il percorso verso il GO pieno è già descritto passo-passo in `TRAMA_ONE_CONTROLLED_PUBLICATION.md` §3 — nessun ulteriore lavoro di implementazione richiesto, solo azioni di Fabrizio | `TRAMA_ONE_CONTROLLED_BETA_GATE_FINAL.md`, DEC-57, DEC-58, DEC-59, DEC-60, DEC-61, DEC-62, DEC-63, DEC-64 |

| DEC-66 | `TRAMA_ONE_VISUAL_ACCEPTANCE.md` (§15) arricchito con passi operativi puntuali per Fabrizio; corretto un errore di ordine nella matrice Spotlight (righe 15/16) | Su richiesta esplicita di Fabrizio dopo il deploy (test `TEST_SCOPE=critical` verde, 94 passed/0 failed/50 skipped), il documento è stato riscritto per essere eseguibile senza conoscenza pregressa di DevTools/Playwright: nuova §0 (3 host di produzione, uno per portale — `buddykids-app`/`buddykids-partner`/`buddykids-admin`.vercel.app — con l'account di test giusto per ciascuno, password mai scritte in chiaro nel documento ma referenziate a `.env.test` locale), nuova §1 (istruzioni Chrome DevTools passo-passo per impostare le 3 larghezze 390/768/1440px), nuova §3 (percorso di navigazione esplicito sezione per sezione, con la sequenza esatta di click reali necessaria per attraversare i 6 step dello Spotlight Partner) e nuova §4 (come resettare il tour Partner, che non ha un bottone "Ricomincia" nell'interfaccia a differenza del Parent — comando `node tests/cleanup-test-data.mjs` verificato leggendo lo script, righe 315-321, o rilancio di `deploy.sh`/`test-deploy.sh` che lo esegue automaticamente). **Errore corretto**: la versione precedente elencava le righe 15/16 della matrice Spotlight come "publish" poi "configure_spot_days" — verificato leggendo `lib/walkthrough/registry.ts` (righe 73-120) che l'ordine reale del percorso è create_activity → configure_weeks → configure_pricing → **configure_spot_days** → **publish** → dashboard: le due righe sono state scambiate per riflettere la sequenza vera, altrimenti le istruzioni operative avrebbero portato a un ordine di navigazione sbagliato. Verificati anche i tag `data-spotlight` reali con grep diretto (`app/center/activities/page.tsx:23`, `ActivityEditForm.tsx:319/568/777`, `calendar/page.tsx:57`) per scrivere istruzioni di navigazione accurate ("clicca dentro la card X"), invece di assumerli dalla sola descrizione testuale della matrice. Nessuna modifica alla matrice PASS/FIX/N-A stessa (contenuto dei Check invariato, solo l'ordine di 2 righe e l'aggiunta delle sezioni operative) | `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, `lib/walkthrough/registry.ts`, `app/center/activities/page.tsx`, `app/center/activities/[id]/ActivityEditForm.tsx`, `app/center/activities/[id]/calendar/page.tsx`, `tests/cleanup-test-data.mjs`, DEC-60, DEC-61 |

| DEC-67 | Primo FIX reale del Visual Acceptance Gate (§15, riga 1): toggle LEGACY/NEXTGEN nascosto su `/one` | Fabrizio ha eseguito §15 seguendo la guida operativa (DEC-66) e ha riportato uno screenshot a ~400px: il titolo "TRAMA ONE — Parent" nell'header di `/one` risultava tagliato/coperto dal pulsante flottante "Passa a NextGen" (`components/VersionToggle.tsx`, `fixed right-4 top-4 z-50`). Verificato che `/one` non era nella lista `HIDDEN_PREFIXES` di quel componente, a differenza di `/center`/`/admin`/`/activity`/`/share` — quest'ultimo escluso in Gate C (28/07) per un bug identico (sovrapposizione con un controllo dedicato nello stesso angolo). Corretto aggiungendo `/one` alla lista: scelta motivata anche dal fatto che `/one` ha già una propria freccia indietro verso `/nextgen` nell'header, rendendo il toggle flottante ridondante oltre che in conflitto visivo. Aggiunto `TC-N614` (`tests/nextgen/version-toggle.spec.ts`) a verifica della regressione. Nell'aggiornare `BuddyKids_Test_Case.xlsx` è stato scoperto un conflitto di ID preesistente e non correlato (due righe `TC-N32` già presenti prima di questo fix) — corretto rinominando la nuova riga in `TC-N614` (primo ID libero dopo l'ultimo blocco usato, 613) per non aggiungere un terzo duplicato; il conflitto originale tra le due vecchie righe TC-N32 resta non toccato, fuori perimetro di questo fix. Verifica statica: `tsc --noEmit` ed `eslint` puliti su `components/VersionToggle.tsx` e `tests/nextgen/version-toggle.spec.ts`; xlsx ricalcolato senza errori (487 formule). Rimane da confermare via il prossimo giro di §15 (righe 2-20 e il resto della riga 1 agli altri 2 breakpoint) che non ci siano altri FIX | `components/VersionToggle.tsx`, `tests/nextgen/version-toggle.spec.ts`, `BuddyKids_Test_Case.xlsx`, `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, DEC-59, DEC-61, DEC-66 |

| DEC-68 | Bug reale nello Spotlight Partner trovato durante §15: target non portato in vista al cambio step -> popover mal posizionato o "perso" | Fabrizio ha proseguito §15 sullo Spotlight (righe 10-20) e riportato 2 screenshot con 3 righe FAIL: riga 11 (popover overflow, screenshot su `/center/activities/new`), riga 14 (`configure_pricing`: "il tutorial è troppo in basso... la pagina deve essere posizionata a quel livello"), riga 15 (`configure_spot_days` sul calendario: "Tour non visibile, l'overlay viene perso totalmente"). Root cause identificata leggendo `lib/spotlight/position.ts`/`components/spotlight/PartnerSpotlight.tsx`: `getBoundingClientRect()` è relativo al VIEWPORT corrente (non al documento), e il componente non scrollava mai il target in vista quando lo step cambiava — se il target reale (es. la card "Servizi extra e pasto", molto più in basso della pagina, o il calendario disponibilità) non era già visibile, `computePopoverPosition` calcolava un `top` che eccedeva `viewport.height`: il popover, con `position: fixed`, risultava non renderizzabile in nessuna posizione di scroll (comportamento identico sia per riga 14 che per riga 15 — la differenza di sintomo, "troppo in basso" vs "perso totalmente", dipende solo da quanto il target era distante dalla viewport iniziale). **Riga 11** (screenshot su `/center/activities/new`) si è rivelato invece comportamento CORRETTO per design: quella pagina non ha il target `configure_weeks` (form diverso da `ActivityEditForm.tsx`, verificato via grep), quindi il badge "target non trovato" (title+description, nessun bottone) è quello atteso da TC-N416 — nessun fix necessario su questo punto, ma la sua forma (badge largo quasi quanto lo schermo su mobile, non un piccolo chip) ha contribuito alla percezione di "popover rotto". Fix applicato in 2 commit: (1) clamp verticale di sicurezza in `computePopoverPosition` (`lib/spotlight/position.ts`) — nuovo test dedicato; (2) fix primario, `el.scrollIntoView({behavior:"smooth", block:"center"})` sul target reale in `PartnerSpotlight.tsx`, eseguito UNA sola volta per cambio di step (tracciato per step-key in un ref dedicato, per non combattere uno scroll manuale dell'utente durante lo stesso step). Verifica statica: `tsc`/`eslint` puliti; suite `tests/one/spotlight-position.spec.ts` 28/28 verde (chromium+mobile-chrome). Fabrizio ha inoltre segnalato una percezione più ampia ("il walkthrough non è lineare, non aiuta nella comprensione, deve essere più evidente") — non trattata come un item unico chiuso qui: lo scroll automatico ne risolve la causa tecnica più concreta (l'utente doveva cercare a occhio l'elemento evidenziato), ma resta da confermare con un nuovo giro di §15 se la sensazione persiste, per eventuali ulteriori iterazioni mirate | `lib/spotlight/position.ts`, `components/spotlight/PartnerSpotlight.tsx`, `tests/one/spotlight-position.spec.ts`, `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, DEC-60, DEC-66, DEC-67 |

| DEC-69 | Spotlight Partner: percorso non sequenziale, testi non corrispondenti al target reale, nessun link esplicito verso il Calendario disponibilità — fix del riscontro di Fabrizio su §15 righe 13-15 (supersede parzialmente l'ordine fissato in DEC-66) | Fabrizio ha chiesto un ricontrollo esplicito che gli step 1-6 fossero "logicamente sequenziali" e che ogni azione richiesta fosse "esplicita, altrimenti si va a tentativi", segnalando in particolare che dopo lo step "Pubblica" ("Salva la scheda da questo momento...") "non si capisce cosa bisogna fare dopo". Investigazione con lettura diretta di `ActivityEditForm.tsx` (righe 300-794) e `app/center/activities/[id]/calendar/page.tsx`: confermati 3 problemi reali. (1) Il testo dello step `configure_weeks` ("Configura le settimane disponibili e la capacità") non corrisponde al suo target reale (`data-spotlight="configure_weeks"`, righe 317-396), che è la card "Informazioni generali" — nome, fascia d'età, prezzo a settimana, navetta, descrizione: nessuna settimana o capacità lì. (2) Analogamente `configure_pricing` (righe 566-673) è la card "Servizi extra e pasto" (ingresso anticipato/uscita posticipata/pasto), non "prezzo"/"navetta" come diceva il testo precedente. (3) L'ordine del percorso (fissato in DEC-66 come create_activity → configure_weeks → configure_pricing → configure_spot_days → publish → dashboard) obbligava a rimbalzare tra la pagina di modifica e quella del calendario (modifica -> calendario -> di nuovo modifica per salvare) senza alcun link nel codice: grep su `ActivityEditForm.tsx`/`app/center/activities/[id]/page.tsx` conferma zero riferimenti a `/calendar` in tutto il flusso di modifica — l'utente doveva scrivere l'URL a mano o tornare indietro per intuizione. Fix in `lib/walkthrough/registry.ts`: (a) riordinati gli step così che `publish` viene ORA prima di `configure_spot_days` (salva prima, poi apri il calendario — la sequenza inversa di DEC-66, corretta di nuovo alla luce del riscontro reale di navigazione), (b) riscritti i testi di `configure_weeks` ("Informazioni di base" — compila nome/età/prezzo/descrizione) e `configure_pricing` ("Servizi extra e pasto" — ingresso anticipato/uscita posticipata/pasto) per rispecchiare l'elemento realmente evidenziato, (c) SOLO i campi `title`/`description` sono stati cambiati, le chiavi tecniche (`key`) restano invariate per non invalidare le righe già scritte in `tutorial_progress` per gli account di test. Aggiunto un meccanismo nuovo, minimo e mirato: campo opzionale `spotlightMissingHint: {suffix, label}` sulla definizione dello step — quando il target reale vive su un'altra pagina, il badge "target non trovato" (`components/spotlight/PartnerSpotlight.tsx`) ora mostra anche un `<Link>` reale verso `pathname corrente + suffix` (usato solo per `configure_spot_days`, che punta a `/calendar`), sotto una guardia che verifica di essere su una pagina di modifica di un'attività reale (regex su `/center/activities/[id]`, esclude `/new` dove non esiste ancora un id). Propagato il nuovo campo anche in `lib/walkthrough/data.ts` (`WalkthroughStepProgress`). Aggiornati i test: `tests/one/walkthrough-partner.spec.ts` TC-N416 ora si aspetta il titolo "Informazioni di base" (non più "Configura le settimane") e verifica che SU `/new` non compaia alcun link (nessuna attività reale, il link porterebbe a un 404); nuovo `TC-N615` verifica il link reale "Vai al Calendario disponibilità →" nel badge quando lo step corrente è `configure_spot_days`, incluso il click che naviga davvero. Aggiornato `BuddyKids_Test_Case.xlsx` (riga TC-N416 e nuova riga TC-N615, xlsx ricalcolato: 488 formule, 0 errori) e `TRAMA_ONE_VISUAL_ACCEPTANCE.md` §3.4/§5.4 (nuovo ordine, nuovi testi, nuova riga 16 per il link, righe rinumerate di conseguenza). Verifica statica: `tsc --noEmit` ed `eslint` puliti su `lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `components/spotlight/PartnerSpotlight.tsx`. Resta da confermare da Fabrizio con un nuovo giro di §15 sulle righe 13-20 (il resto della matrice, righe 1-12, non è toccato da questo fix) | `lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `components/spotlight/PartnerSpotlight.tsx`, `tests/one/walkthrough-partner.spec.ts`, `BuddyKids_Test_Case.xlsx`, `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, DEC-60, DEC-66, DEC-68 |

| DEC-70 | Terzo giro di riscontro Fabrizio su §15: badge sovrapposto a pulsanti reali, cutout con angoli fissi, popover "dashboard" rotto su mobile (bug reale) — più riavvio tour da Impostazioni e chiarimento su Spotlight lato Parent | Fabrizio ha riportato 3 screenshot: (1) badge "target non trovato" ("Informazioni di base") sovrapposto al pulsante "Crea attività" su `/center/activities/new` a 390px; (2) il popover dello step "Monitora dalla dashboard" copre quasi tutto lo schermo a 390px, il vero nav item "Dashboard" non è visibile né evidenziato (funziona invece a 768/1440); (3) richiesta che il cutout segua la forma reale del target invece di angoli fissi. Root cause di (2), la più seria — bug reale, non solo estetico: il nav item "Dashboard" (`data-spotlight="dashboard"`, `components/dashboard/DashboardLayout.tsx`) è renderizzato DUE VOLTE nel DOM, una copia nella sidebar desktop (`<aside className="... hidden ... md:flex">`, sempre presente ma `display:none` sotto il breakpoint md=768px) e una nel cassetto/drawer mobile (esiste solo quando `drawerOpen===true`). `document.querySelector` in `PartnerSpotlight.tsx::measure()` prendeva sempre il PRIMO elemento nel DOM a prescindere dalla visibilità — su mobile era sempre la copia nascosta, il cui `getBoundingClientRect()` è un rettangolo degenere (0,0,0,0) perché un elemento `display:none` non ha una scatola di layout: `computePopoverPosition` interpretava quel target come "in alto a sinistra con spazio enorme sotto", producendo un popover ancorato lì che finiva per coprire quasi tutto lo schermo senza mai evidenziare l'elemento reale. Fix: nuova funzione pura `pickVisibleTargetIndex` (`lib/spotlight/position.ts`) — dato un array di rect letti da TUTTI i candidati con lo stesso `data-spotlight`, ritorna l'indice del primo con area non nulla, o -1 se nessuno è visibile; in quel caso (es. cassetto mobile chiuso) lo step ricade correttamente nel badge "target non trovato" invece di un popover rotto — comportamento accettato come noto: su mobile lo step "dashboard" richiede di aprire prima il menu (☰). 5 nuovi test unitari "no browser" (regressione diretta di questo bug) in `tests/one/spotlight-position.spec.ts`, più `TC-N616` nell'xlsx. Fix (1): il badge era `fixed bottom-5 right-5`, angolo dove le pagine Partner mettono quasi sempre la loro azione primaria (submit di form corti come `/new`, "Salva modifiche"...) — spostato in alto a destra (`top-20 right-4`, sotto l'header mobile), zona mai usata per azioni cliccabili in nessuna pagina esistente. Fix (3): il ring del cutout aveva un raggio fisso (`rounded-lg`, 8px) — ora legge `getComputedStyle(el).borderRadius` nel momento della misurazione e lo applica via `style` inline, seguendo la forma reale del target (es. pulsanti pillola `rounded-full`). Fabrizio ha anche chiesto se il tour si può riavviare dalle Impostazioni: la server action `restartWalkthroughAction` (`app/actions/walkthrough.ts`) esisteva già ed è generica per costruzione, ma non era raggiungibile da nessun bottone reale per il Partner (solo da script `node tests/cleanup-test-data.mjs`, o dal "Ricomincia il percorso" dentro `WalkthroughCard.tsx`, visibile SOLO a percorso Parent già concluso). Aggiunto un nuovo componente standalone e riusabile `components/WalkthroughRestartButton.tsx`, montato in `app/center/account/preferenze/page.tsx` sotto una sezione "Tour guidato" — visibile SOLO se `TRAMA_ONE_ENABLED` risolve vero per l'utente (stesso gating già usato per lo Spotlight stesso in `app/center/layout.tsx`, per non mostrare un bottone inutile a chi non ha il percorso attivo). E se lo stesso Spotlight è previsto lato Parent: risposta onesta comunicata a Fabrizio, non implementata qui — `welcome_parent` (il percorso Parent) usa ancora la vecchia `WalkthroughCard.tsx` testuale su `/one`, non il vero overlay ancorato al DOM introdotto per il Partner in questo gate (§7-14). Il motore generico (registry/data/actions) supporta già entrambi i pattern per costruzione, ma costruire l'equivalente Spotlight per il Parent è un lavoro nuovo (individuare i target reali nelle pagine Parent, taggarli `data-spotlight`, definire un percorso) fuori dal perimetro di questo fix — da pianificare come sprint a parte se Fabrizio lo conferma. Verifica statica: `tsc --noEmit` ed `eslint` puliti su tutti i file toccati; `tests/one/spotlight-position.spec.ts` 38/38 verde (chromium+mobile-chrome, inclusi i 5 nuovi test); xlsx ricalcolato (489 formule, 0 errori) | `lib/spotlight/position.ts`, `components/spotlight/PartnerSpotlight.tsx`, `components/WalkthroughRestartButton.tsx`, `app/center/account/preferenze/page.tsx`, `tests/one/spotlight-position.spec.ts`, `BuddyKids_Test_Case.xlsx`, `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, DEC-60, DEC-68, DEC-69 |

| DEC-71 | Quarto giro di riscontro Fabrizio su §15: click su un campo reale dentro una card multi-campo avanzava lo step prematuramente, impedendo di finire la configurazione | Fabrizio: "nel tutorial, se clicco una delle impostazioni dello step (es. servizi extra clicco ingresso anticipato..) va direttamente nello step successivo e non permette di finire la configurazione". Root cause verificata leggendo il click-capture listener in `PartnerSpotlight.tsx` (un `document.addEventListener("click", ..., true)` che completa lo step al primo click GENUINO su qualunque elemento dentro `[data-spotlight="..."]`, meccanismo introdotto in Sprint 6/DEC-60 per gli step il cui target è un singolo pulsante/link) combinata con la lettura di `ActivityEditForm.tsx` (righe 560-673, 805-852): i target `configure_weeks`/`configure_pricing` sono intere card con più campi (checkbox "Ingresso anticipato (pre-servizio)"/"Uscita posticipata (post-servizio)", select Pasto, altre checkbox), non un singolo pulsante — il primo click su QUALUNQUE campo (anche solo per aprire le opzioni orario di un servizio extra) faceva scattare l'avanzamento, impedendo di fatto di compilare gli altri campi della stessa card. Grep su `AvailabilityCalendar.tsx` (onClick alle righe 109, 153, 238, 260 — selezione giorno, deselezione, salvataggio) ha confermato lo stesso problema di classe per `configure_spot_days`, esteso proattivamente allo stesso fix pur non essendo stato riportato esplicitamente da Fabrizio, perché il target (il calendario disponibilità) è anch'esso multi-cella cliccabile. Fix: nuovo campo opzionale `spotlightManualAdvance?: boolean` in `WalkthroughStepDefinition` (`lib/walkthrough/registry.ts`) e propagato in `WalkthroughStepProgress` (`lib/walkthrough/data.ts`); impostato `true` su `configure_weeks`, `configure_pricing`, `configure_spot_days` — lasciato assente (comportamento invariato) su `create_activity`/`publish`/`dashboard`, i cui target SONO l'azione stessa da completare. In `PartnerSpotlight.tsx`: il listener di click-capture ora esce subito (`if (current.spotlightManualAdvance) return;`) per questi step, e il popover in stato "in_progress" mostra un pulsante esplicito **"Ho finito, continua →"** (oltre a "Salta per ora") invece del testo "Fatto? Clicca l'elemento evidenziato per continuare." — solo quel click chiama `handleComplete()`. Aggiornato `tests/one/walkthrough-partner.spec.ts`: il test `TC-N615` (che attraversa la sequenza configure_weeks→configure_pricing→publish→configure_spot_days) è stato riscritto per usare il nuovo flusso "Inizia" + "Ho finito, continua →" sui due step manuali, e integra ora anche la verifica diretta del bug segnalato — un click reale sulla checkbox "Ingresso anticipato (pre-servizio)" dentro "Servizi extra e pasto" NON deve far avanzare lo step, verificato con `expect(pricingDialog).toBeVisible()` subito dopo il click sul campo, prima del click esplicito sul pulsante. Aggiornato `BuddyKids_Test_Case.xlsx`: riga `TC-N615` riscritta (Precondizioni/Passi/Risultato atteso/Note) e nuova riga `TC-N617` aggiunta subito dopo `TC-N616` a copertura dedicata dello stesso scenario negativo (xlsx ricalcolato: 490 formule, 0 errori). Aggiornato `TRAMA_ONE_VISUAL_ACCEPTANCE.md` §3.4 (punti 3-7 riscritti per descrivere il flusso "Inizia"/"Ho finito, continua →") e §5.4 (righe 13/14/16/20 aggiornate, nuova riga 22 dedicata alla verifica "click su campo non avanza"). Verifica statica: `tsc --noEmit` ed `eslint` puliti su tutti i file toccati (`lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `components/spotlight/PartnerSpotlight.tsx`, `tests/one/walkthrough-partner.spec.ts`); `tests/one/spotlight-position.spec.ts` (38/38, funzioni pure non toccate da questo fix, nessuna regressione) verificato verde in questo stesso giro; `tests/one/walkthrough-partner.spec.ts` non eseguibile in locale in questo ambiente (richiede un browser Chromium con dipendenze di sistema non installate nel sandbox — stessa limitazione preesistente, non introdotta da questo fix), verificabile solo da Fabrizio col prossimo `bash test-deploy.sh`/`deploy.sh` contro un deploy reale. Resta da confermare da Fabrizio con un nuovo giro di §15 sulle righe 13/14/16/20/22 | `lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `components/spotlight/PartnerSpotlight.tsx`, `tests/one/walkthrough-partner.spec.ts`, `BuddyKids_Test_Case.xlsx`, `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, DEC-60, DEC-69, DEC-70 |

| DEC-72 | Quinto giro di riscontro Fabrizio su §15: focus rubato al campo mentre si compila, badge "target non trovato" completamente invisibile in due scenari reali, "Inizia" ripetuto senza motivo per ogni step | Fabrizio ha testato il deploy di DEC-71 e riportato 3 problemi, verbatim: "è corretto che non possa modificare posti rimasti e navetta € e prezzo a settimana? se clicco nel campo il cursore poi sembra sparire..anche sulla scelta del pasto nello step3 non funziona benissimo: appare ma non riesco a selezionare per tempo", "nello step 4 'inizia' non ha senso..ci vuole coerenza", "dopo aver fato salva non mi fa capire che devo andare nel 'Calendario disponibilità'". Investigazione con lettura diretta e completa di `components/spotlight/PartnerSpotlight.tsx`, confermati 3 bug reali distinti. (1) **Focus rubato** (bug bloccante, introdotto/esposto da DEC-71): l'effetto `useEffect(() => { if (targetRect && popoverRef.current) popoverRef.current.focus(); }, [targetRect, current?.key])` dipende da `targetRect`, ma `measure()` (eseguito ogni 500ms via `setInterval`, PIÙ su ogni resize/scroll) chiamava `setTargetRect({...})` con un oggetto letterale NUOVO a ogni chiamata anche quando il rettangolo non era cambiato — React confronta lo state per identità di riferimento, quindi l'effetto si riattivava ogni ~500ms, rifocalizzando il popover e rubando il focus a qualunque campo reale l'utente stesse usando (input di testo: cursore che sparisce; `<select>` nativo: il menu si chiude perché perde focus, spiegando "appare ma non riesco a selezionare per tempo"). Questo bug esisteva ANCHE PRIMA di DEC-71, ma era invisibile: sugli step click-based il primo click completava subito lo step (comportamento allora corretto), quindi nessuno restava mai abbastanza a lungo su un campo perché il furto di focus periodico diventasse percepibile — DEC-71, lasciando intenzionalmente l'utente libero di compilare più campi con calma, ha reso il bug preesistente osservabile per la prima volta. Fix a due livelli: `setTargetRect` ora usa una funzione di update che ritorna la STESSA referenza se i 4 valori (top/left/width/height) sono identici al render precedente (React non rirenderizza né riattiva effetti quando il setter ritorna lo stesso riferimento); in aggiunta, l'effetto di focus ora si attiva una sola volta per step (nuovo `focusedStepRef`, stesso pattern già usato per `scrolledStepRef`/DEC-68), difesa aggiuntiva contro cambi legittimi del rettangolo (scroll, resize/tastiera virtuale) che non devono comunque rubare il focus una volta che il popover è già apparso per quello step. (2) **Badge "target non trovato" completamente invisibile** (bug bloccante, precedente a questa sessione — la logica risale a Sprint 6/DEC-60): in `measure()`, quando la pagina corrente non corrisponde affatto allo `spotlightRoute` dello step (`matchesSpotlightRoute` ritorna `false`), il codice chiamava `setTargetMissing(false)` — un booleano INVERTITO rispetto all'intento: più sotto, `if (!targetRect && !targetMissing) return null` fa sì che l'INTERO componente non renderizzi nulla in quel caso, invece di mostrare il badge di fallback (con l'eventuale link di `spotlightMissingHint`, introdotto proprio per questo scenario in DEC-69). Root cause del riscontro "dopo aver fatto salva non mi fa capire che devo andare nel Calendario disponibilità": lo step `configure_spot_days` ha `spotlightRoute: "/center/activities/*/calendar"`, che NON combacia con `/center/activities/[id]` (la pagina su cui si resta subito dopo il salvataggio, prima di navigare al calendario) — quindi il badge con il link non è MAI comparso, in nessun deploy precedente, nonostante DEC-69 lo avesse "implementato" e TC-N615 lo "verificasse": il test non è mai stato eseguito contro un browser reale in questo ambiente (mancano le dipendenze di sistema Playwright nel sandbox), quindi il bug è rimasto invisibile fino al primo test manuale reale di Fabrizio. Stesso bug booleano colpisce anche la riga 18 della matrice Visual Acceptance (badge senza link su una pagina qualunque senza target) — mai realmente verificabile prima d'ora per lo stesso motivo, mascherato inoltre dal fatto che gli step effettivamente testati manualmente in precedenza (`create_activity`/`configure_weeks`) usano un pattern di route `"prefisso*"` che in pratica combacia quasi sempre (`endsWith("")` è sempre vero), non attraversando mai il ramo buggato. Fix: un carattere, `setTargetMissing(false)` → `setTargetMissing(true)` nel ramo di route non corrispondente — il badge di fallback (con o senza link a seconda dello step) ora compare correttamente in entrambi gli scenari. (3) **"Inizia" ripetuto senza motivo** (miglioramento UX, non un bug in senso stretto): Fabrizio, verbatim, "nello step 4 'inizia' non ha senso..ci vuole coerenza" — prima di questo fix OGNI step, non solo il primo, nasceva "not_started" e richiedeva un click esplicito su "Inizia" prima di poter interagire, anche subito DOPO che l'utente aveva appena completato o saltato lo step precedente (quindi era già chiaramente e deliberatamente dentro il percorso guidato). Verificato inoltre che questo gate non aveva MAI avuto una funzione protettiva reale per gli step click-based: il listener di completamento su click (`document.addEventListener("click", ...)`) si attiva indipendentemente dallo status dello step (non controlla `not_started` vs `in_progress`), quindi cliccare l'elemento reale avrebbe sempre completato lo step comunque, "Inizia" o no — puro attrito UI senza alcuno scopo per quegli step. Fix in `nextStepAfter` (`PartnerSpotlight.tsx`): quando si passa allo step successivo, se questo è ancora `"not_started"`, viene ora avviato automaticamente (stessa azione server `startWalkthroughStepAction` già usata da `handleStart`, più aggiornamento ottimistico locale a `"in_progress"`) — il popover mostra quindi subito "Ho finito, continua →" (step manuali) o il testo click-based, senza un secondo "Inizia" superfluo. Il gate "Inizia" resta SOLO per il primissimo step mostrato al mount iniziale (`create_activity`), un opt-in esplicito e unico all'intero percorso guidato — comportamento invariato e già coperto da TC-N414/TC-N415, non toccati da questo fix. `nextStepAfter` è stato inoltre convertito da funzione semplice a `useCallback` (dipendenza `[progress]`) per correggere un warning `react-hooks/exhaustive-deps` introdotto insieme al fix (era chiamata dentro `handleComplete`, anch'esso un `useCallback`, senza essere elencata tra le sue dipendenze). Verificato che TUTTE le 3 correzioni sono compatibili con il local build (`npm run build` completato pulito in ~35s, nessun hang, nessun errore — confermando anche che il timeout di build da 45 minuti riscontrato da Fabrizio su Vercel poco prima era un problema infrastrutturale transitorio, non di codice, come già verificato e comunicato separatamente). Aggiornato `tests/one/walkthrough-partner.spec.ts`: `TC-N615` riscritto per non cliccare più "Inizia" su `configure_weeks`/`configure_pricing` (asserendo esplicitamente `toHaveCount(0)` per quel pulsante), mantenendo la verifica DEC-71 del click sul campo reale che non avanza lo step. Aggiornato `BuddyKids_Test_Case.xlsx`: riga `TC-N615` riscritta, nuove righe `TC-N618` (verifica manuale del non-furto di focus, difficile da automatizzare in modo affidabile con Playwright headless per via del timing) e `TC-N619` (verifica del badge in entrambi gli scenari di route non corrispondente) — xlsx ricalcolato: 492 formule, 0 errori. Aggiornato `TRAMA_ONE_VISUAL_ACCEPTANCE.md` §3.4 (sequenza operativa riscritta senza i passaggi "Inizia" superflui, avvertenze esplicite sulle righe 13/14/16/18/22 di verificare con attenzione i 3 bug appena corretti) e §5.4 (righe 10/13/14/15/16/17/18/22 aggiornate). Verifica statica: `tsc --noEmit` ed `eslint` puliti su tutti i file toccati (`components/spotlight/PartnerSpotlight.tsx`, `tests/one/walkthrough-partner.spec.ts`); `tests/one/spotlight-position.spec.ts` non impattato da questo fix (nessuna funzione pura nuova, tutta la logica corretta vive nel componente React, non estraibile in funzioni pure aggiuntive senza un refactoring più ampio). Resta da confermare da Fabrizio con un nuovo giro di §15, in particolare le righe 13/14/16/18/22 dove i 3 bug erano osservabili | `components/spotlight/PartnerSpotlight.tsx`, `tests/one/walkthrough-partner.spec.ts`, `BuddyKids_Test_Case.xlsx`, `TRAMA_ONE_VISUAL_ACCEPTANCE.md`, DEC-60, DEC-69, DEC-71 |

| DEC-73 | Sesto giro di riscontro Fabrizio su §15: cutout ancora "squadrato" nonostante DEC-70, flash di posizione sbagliata nella transizione tra step, badge "dashboard" senza indizio su come renderlo visibile su mobile, sottotitolo "Preferenze" da aggiornare | Fabrizio, verbatim, dopo aver testato il deploy con DEC-72 live: "Nel passaggio da step 3 a 4 la schermata del walkthrough cambia ma non son riuscito a registrarlo..comunque è sovrapposto e nella schermata dei giorni spot c'è qualcosa che nonf unziona..l'elemento dashboard non è evidenziato..l'ho visto solo aprendo il menu laterale.. Come vedi il riquadro intorno ai pulsanti (vedi primo screenshot) è ancora squadrato ci sono gli angoli.. Nel sottotitolo del menu 'Preferenze' va indicato anche tour guidato" (3 screenshot allegati, popover degli step "Pubblica"/"Configura i Giorni spot"/"Monitora dalla dashboard"). Investigati e corretti 4 problemi distinti. (1) **Cutout ancora squadrato** (bug reale, root cause matematica non trovata in DEC-70): il ring legge il border-radius REALE del target via `getComputedStyle` (fix DEC-70), ma il cutout stesso NON è il rettangolo del target — è quel rettangolo INGRANDITO di 8px su ogni lato (`computeCutoutRect`, padding di default 8) — e il ring veniva disegnato con lo STESSO raggio numerico non modificato. Ingrandire un rettangolo arrotondato uniformemente in ogni direzione senza aumentare il raggio dei suoi angoli della stessa quantità appiattisce visivamente la curvatura: il bottone "Salva modifiche" (`rounded-md`, 6px) resta a 6px sul cutout, ma un raggio di 6px è quasi impercettibile su un box alto ~56px (40px del bottone + 16px di padding) invece dei ~40px originali — esattamente "ancora squadrato ci sono gli angoli". Fix: nuova funzione pura `padBorderRadius(radius, padding = CUTOUT_PADDING)` in `lib/spotlight/position.ts` — per mantenere la stessa curvatura visiva quando un rettangolo arrotondato viene offsettato verso l'esterno di `padding` in ogni direzione, il raggio dei suoi angoli va aumentato della stessa quantità (raggio_nuovo = raggio_originale + padding); gestisce anche shorthand a più valori (angoli non uniformi, ogni token px incrementato singolarmente) e token non in px (es. "%", lasciati invariati). Estratta anche una costante condivisa `CUTOUT_PADDING = 8` (prima hardcoded separatamente in `computeCutoutRect` e nel valore da sommare), unica fonte di verità tra le due funzioni. Usata in `PartnerSpotlight.tsx`: `borderRadius: padBorderRadius(cutoutRadius)` al posto di `borderRadius: cutoutRadius`. 6 nuovi test unitari "no browser" in `tests/one/spotlight-position.spec.ts` (incluso il caso reale 6px→14px e la verifica che un bottone pillola, 9999px, resti visivamente una pillola anche dopo il padding — il CSS clampa comunque a metà altezza). (2) **Flash di posizione sbagliata nella transizione tra step** (bug reale, introdotto dall'auto-start di DEC-72): quando `currentKey` cambia (`nextStepAfter`), `targetRect`/`targetMissing` restavano quelli dello step APPENA CONCLUSO fino al tick successivo di `measure()` (schedulato via `requestAnimationFrame`, un frame dopo) — in quella finestra React renderizzava già titolo/descrizione del NUOVO step ma ancorati alla POSIZIONE del vecchio target, un frame di popover/cutout disallineati o sovrapposti a un elemento che non c'entra — troppo breve per uno screenshot ("non son riuscito a registrarlo") ma visibile a occhio, spiegando "la schermata cambia... è sovrapposto". Fix: pattern React consigliato per "adjusting state while rendering" — confronto tra `currentKey` e l'ultimo valore renderizzato (`lastMeasuredKey`, `useState`), azzeramento SINCRONO di `targetRect`/`targetMissing` durante il render stesso (non in un effetto, per evitare sia il flash sia il warning `react-hooks/set-state-in-effect` di un setState sincrono dentro un effetto — un primo tentativo con `useLayoutEffect` è stato scartato proprio per questo lint error) — il componente ritorna `null` per quel singolo render invece di mostrare la posizione sbagliata, poi la prossima `measure()` (già schedulata) aggiorna con la posizione corretta del nuovo target. Verifica manuale richiesta (timing di un singolo frame, non automatizzabile in modo affidabile con Playwright). (3) **Badge "dashboard" senza indizio su mobile** (miglioramento UX/chiarezza, non un bug di rendering): lo step finale "dashboard" (`spotlightRoute: "*"`, sempre matcha) su mobile richiede il cassetto laterale aperto per essere misurabile (comportamento DOCUMENTATO e intenzionale, DEC-70) — a cassetto chiuso il badge "target non trovato" compariva comunque (titolo+descrizione), ma senza alcun indizio su COME renderlo visibile: Fabrizio l'ha trovato "solo aprendo il menu laterale" per tentativi. A differenza di `configure_spot_days` (che richiede di NAVIGARE a un'altra pagina, coperto da `spotlightMissingHint` con un link, DEC-69), qui l'azione richiesta è nella STESSA pagina (aprire il cassetto), un link non ha senso. Fix: nuovo campo opzionale `spotlightMissingNote?: string` in `WalkthroughStepDefinition` (`lib/walkthrough/registry.ts`) e propagato in `WalkthroughStepProgress` (`lib/walkthrough/data.ts`) — nota testuale (nessun link) mostrata SOLO nel badge di fallback, per non appesantire la descrizione normale mostrata quando il target è già visibile; valorizzato per lo step "dashboard": "Su schermi piccoli, apri il menu ☰ in alto per trovare la voce \"Dashboard\" nel cassetto laterale." Renderizzata in `PartnerSpotlight.tsx` sotto l'eventuale link di `spotlightMissingHint`. Test "no browser" nuovo in `tests/one/walkthrough-partner.spec.ts` (`TC-N620`, verifica diretta del dato nel registry — il rendering reale del badge su mobile con drawer chiuso richiederebbe di completare l'intero percorso fino allo step "dashboard" con un browser reale, troppo fragile/lento da automatizzare qui in modo affidabile). (4) **Sottotitolo "Preferenze" senza menzione del tour guidato** (richiesta di copy esplicita): `ProfileSettingsSection.tsx` è condiviso identico tra profilo Genitore (`basePath="/profile"`) e "Il mio account" del Gestore (`basePath="/center/account"`) — ma SOLO la pagina Preferenze del Gestore monta `WalkthroughRestartButton` (gated da `TRAMA_ONE_ENABLED`, introdotto in DEC-70), lato Genitore non esiste ancora alcun tour da riavviare (Spotlight Parent non costruito, task #441 ancora in backlog). Fix: sottotitolo della voce "Preferenze" reso condizionale su `basePath` — `"Lingua, tema, notifiche, tour guidato"` se `basePath.startsWith("/center")`, altrimenti invariato (`"Lingua, tema, notifiche"`), per non promettere lato Genitore una funzione assente. Aggiornato `tests/gestore/account.spec.ts` (TC-138) per asserire il nuovo testo completo lato Gestore; `tests/genitori/profilo.spec.ts` non toccato (il sottotitolo lato Genitore non cambia). Verifica statica: `tsc --noEmit` ed `eslint --no-cache` puliti su tutti i file toccati; `npm run build` completato pulito; `tests/one/spotlight-position.spec.ts` (106/106, incluso mobile-chrome) e `tests/one/walkthrough-partner.spec.ts` TC-N620 (2/2, chromium+mobile-chrome) verdi in locale. Aggiornato `BuddyKids_Test_Case.xlsx`: righe `TC-N620`/`TC-N621`/`TC-N622`/`TC-N623` aggiunte (xlsx ricalcolato: 496 formule, 0 errori). Resta da confermare da Fabrizio con un nuovo giro live, in particolare (2) e (3) che richiedono verifica visiva/manuale non automatizzabile in questo ambiente | `lib/spotlight/position.ts`, `components/spotlight/PartnerSpotlight.tsx`, `lib/walkthrough/registry.ts`, `lib/walkthrough/data.ts`, `components/ProfileSettingsSection.tsx`, `tests/one/spotlight-position.spec.ts`, `tests/one/walkthrough-partner.spec.ts`, `tests/gestore/account.spec.ts`, `BuddyKids_Test_Case.xlsx`, DEC-69, DEC-70, DEC-72 |

## Nota

Ogni nuova decisione futura va aggiunta in coda a questo log con numero progressivo, mai sovrascrivendo le voci esistenti. Se una decisione precedente viene superata, la nuova voce deve citare esplicitamente il numero della decisione che sostituisce.
